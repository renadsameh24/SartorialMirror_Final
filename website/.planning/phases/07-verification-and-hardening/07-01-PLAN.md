---
phase: 07-verification-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/session/sessionStore.test.ts
  - src/stores/selectors.test.ts
  - src/stores/catalog/catalogStore.test.ts
  - src/stores/measurements/measurementsStore.test.ts
  - src/stores/fit/fitStore.test.ts
  - src/lib/sessionReset/resetSession.test.ts
  - src/stores/session/sessionStore.ts
  - src/stores/catalog/catalogStore.ts
  - src/stores/measurements/measurementsStore.ts
  - src/stores/fit/fitStore.ts
  - src/lib/sessionReset/resetSession.ts
  - src/lib/sessionReset/resettableStores.ts
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - Core shopper state transitions, selection state, measurement state, fit state, and centralized reset behavior are explicitly verified rather than inferred through later UI tests.
    - Reset and privacy proof remains data-minimizing: shopper-scoped state clears deterministically while app-scoped catalog definitions, system health, admin state, and uiMode survive.
    - Any fixes discovered during this plan stay local to the existing store/reset layer and do not redesign the approved Phase 3 architecture.
  artifacts:
    - src/stores/catalog/catalogStore.test.ts
    - src/stores/measurements/measurementsStore.test.ts
    - src/stores/fit/fitStore.test.ts
    - src/stores/session/sessionStore.test.ts
    - src/stores/selectors.test.ts
    - src/lib/sessionReset/resetSession.test.ts
  key_links:
    - src/lib/sessionReset/resetSession.ts remains the only authority that completes shopper reset.
    - src/lib/sessionReset/resettableStores.ts defines the locked shopper reset boundary and order.
    - src/stores/session/sessionStore.ts remains the canonical shopper machine and session identity owner.
---

<objective>
Add risk-based automated proof for the state/store/reset layer so Phase 7 can demonstrate that shopper state correctness and deterministic privacy reset are real system guarantees rather than side effects of screen tests.

Purpose: Close the weakest remaining proof in the store/reset layer before runtime and UI hardening build on top of it.
Output: Focused store-level and reset-level tests, plus any minimal contained fixes required by findings.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-verification-and-hardening/07-CONTEXT.md
@.planning/phases/07-verification-and-hardening/07-DISCUSSION-LOG.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@src/stores/session/sessionStore.ts
@src/stores/catalog/catalogStore.ts
@src/stores/measurements/measurementsStore.ts
@src/stores/fit/fitStore.ts
@src/lib/sessionReset/resetSession.ts
@src/lib/sessionReset/resettableStores.ts
@src/stores/session/sessionStore.test.ts
@src/stores/selectors.test.ts
@src/lib/sessionReset/resetSession.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add direct store tests for catalog, measurements, and fit state ownership</name>
  <files>src/stores/catalog/catalogStore.test.ts, src/stores/measurements/measurementsStore.test.ts, src/stores/fit/fitStore.test.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts</files>
  <read_first>src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/selectors.test.ts, .planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, .planning/phases/07-verification-and-hardening/07-CONTEXT.md</read_first>
  <action>Create focused store-level test files for catalog, measurements, and fit. In `src/stores/catalog/catalogStore.test.ts`, verify that `setSnapshot()` preserves entity ownership, selection survives a snapshot refresh when the selected garment is still present, unavailable snapshots do not silently erase the last good entity set, and `resetSessionState()` clears shopper selection and focus only. In `src/stores/measurements/measurementsStore.test.ts`, verify transitions between `idle`, `partial`, `ready`, and `unavailable`, replacement of snapshots by timestamped payloads, and that `resetSessionState()` clears shopper measurement snapshots completely. In `src/stores/fit/fitStore.test.ts`, verify `idle`, `partial`, `ready`, and `unavailable` handling, qualitative recommendation persistence rules, and that `resetSessionState()` clears recommendation state fully. If a test exposes a correctness gap, fix only the affected store file without changing store ownership or adding new domains.</action>
  <verify>npm run test -- --run src/stores/catalog/catalogStore.test.ts src/stores/measurements/measurementsStore.test.ts src/stores/fit/fitStore.test.ts</verify>
  <acceptance_criteria>
    - `src/stores/catalog/catalogStore.test.ts` exists and contains assertions for snapshot refresh persistence, unavailable snapshot behavior, and `resetSessionState()`.
    - `src/stores/measurements/measurementsStore.test.ts` exists and contains assertions for `partial`, `ready`, `unavailable`, and reset clearing.
    - `src/stores/fit/fitStore.test.ts` exists and contains assertions for `partial`, `ready`, `unavailable`, and reset clearing.
    - Any source-file fix remains inside `src/stores/catalog/catalogStore.ts`, `src/stores/measurements/measurementsStore.ts`, or `src/stores/fit/fitStore.ts` only.
  </acceptance_criteria>
  <done>The data-bearing shopper stores have direct ownership tests for the behaviors that Phase 7 must trust during reset and degraded flows.</done>
</task>

<task type="auto">
  <name>Task 2: Expand session and selector tests around reset-sensitive state transitions</name>
  <files>src/stores/session/sessionStore.test.ts, src/stores/selectors.test.ts, src/stores/session/sessionStore.ts</files>
  <read_first>src/stores/session/sessionStore.ts, src/stores/session/sessionStore.test.ts, src/stores/selectors.test.ts, .planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, .planning/phases/05-shopper-flow-screens/05-CONTEXT.md</read_first>
  <action>Extend `src/stores/session/sessionStore.test.ts` so it covers repeated `endSession()` calls, adoption of runtime session IDs while already in `sessionEnd`, and the locked rule that invalid transitions stay no-op without mutating session identity. Extend `src/stores/selectors.test.ts` so it proves shopper-safe selectors stay reset-safe: `selectCanStartSession()` and `selectCanEndSession()` flip correctly around `sessionEnd`, catalog try-on readiness drops after reset, and fit/measurement selector output returns to empty shopper-safe values after reset rather than stale display rows or stale recommendations. If a failing test reveals a local machine or selector bug, fix only the affected store/selector implementation.</action>
  <verify>npm run test -- --run src/stores/session/sessionStore.test.ts src/stores/selectors.test.ts</verify>
  <acceptance_criteria>
    - `src/stores/session/sessionStore.test.ts` contains assertions for repeated `endSession()`, runtime session ID adoption in `sessionEnd`, and invalid-transition no-ops.
    - `src/stores/selectors.test.ts` contains assertions that catalog, measurement, and fit selector outputs are empty or non-ready again after reset.
    - No source-file change introduces a new shopper phase, new store, or route-based behavior.
  </acceptance_criteria>
  <done>The canonical session machine and UI-safe selectors are explicitly proved around reset-sensitive edge cases.</done>
</task>

<task type="auto">
  <name>Task 3: Harden deterministic privacy reset and prove the exact reset boundary</name>
  <files>src/lib/sessionReset/resetSession.test.ts, src/lib/sessionReset/resetSession.ts, src/lib/sessionReset/resettableStores.ts</files>
  <read_first>src/lib/sessionReset/resetSession.ts, src/lib/sessionReset/resettableStores.ts, src/lib/sessionReset/resetSession.test.ts, src/stores/catalog/catalogStore.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/admin/adminStore.ts, src/stores/uiMode/uiModeStore.ts, .planning/phases/07-verification-and-hardening/07-CONTEXT.md</read_first>
  <action>Extend `src/lib/sessionReset/resetSession.test.ts` to verify the exact reset order in `SHOPPER_RESET_ORDER`, idempotent behavior when reset is invoked after the machine is already idle, and the locked boundary that catalog definitions, system-health snapshots, admin access state, and uiMode survive while shopper selection, measurements, fit, degraded guidance, and session identity do not. Add a regression assertion that post-reset state contains no shopper-derived copyable values from the prior session. If needed, tighten `src/lib/sessionReset/resetSession.ts` or `src/lib/sessionReset/resettableStores.ts`, but do not broaden or shrink the reset boundary without evidence from the test failure.</action>
  <verify>npm run test -- --run src/lib/sessionReset/resetSession.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/sessionReset/resetSession.test.ts` contains assertions for reset order, idle/idempotent invocation, shopper-state clearing, and app-state persistence.
    - `src/lib/sessionReset/resettableStores.ts` still lists only `degraded`, `fit`, `measurements`, `catalog`, and `session` in `SHOPPER_RESET_ORDER`.
    - `src/lib/sessionReset/resetSession.ts` remains the only function that completes the shopper reset sequence.
  </acceptance_criteria>
  <done>Deterministic privacy reset is proved at the exact boundary Phase 7 promises to hand off.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/stores/session/sessionStore.test.ts src/stores/selectors.test.ts src/stores/catalog/catalogStore.test.ts src/stores/measurements/measurementsStore.test.ts src/stores/fit/fitStore.test.ts src/lib/sessionReset/resetSession.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "describe\\('catalogStore'|describe\\('measurementsStore'|describe\\('fitStore'" src/stores/*.test.ts src/stores/**/*.test.ts`
- [ ] `rg -n "SHOPPER_RESET_ORDER|resetSessionState|completeReset" src/lib/sessionReset/*.ts src/stores/**/*.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No architecture drift introduced
- The state/reset layer has direct automated proof for the exact privacy and correctness guarantees Phase 7 is supposed to certify
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-and-hardening/07-01-SUMMARY.md`
</output>
