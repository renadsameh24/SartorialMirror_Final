---
phase: 07-verification-and-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/app/runtime/createAppRuntime.test.ts
  - src/app/runtime/runtimeLifecycle.test.ts
  - src/lib/runtime/normalization.test.ts
  - src/lib/runtime/staleEventGuard.test.ts
  - src/lib/runtime/degradedDerivation.test.ts
  - src/lib/runtime/readModels.test.ts
  - src/adapters/catalog/catalogSnapshotLoader.test.ts
  - src/adapters/catalog/createIntegrationCatalogPort.test.ts
  - src/adapters/runtime/runtimeTransport.test.ts
  - src/adapters/runtime/createIntegrationRuntimePort.test.ts
  - src/adapters/unity/unityTransport.test.ts
  - src/adapters/unity/createIntegrationUnityPort.test.ts
  - src/app/runtime/createAppRuntime.ts
  - src/lib/runtime/applyInboundEvent.ts
  - src/lib/runtime/normalizeCatalogEvent.ts
  - src/lib/runtime/normalizeRuntimeEvent.ts
  - src/lib/runtime/normalizeUnityEvent.ts
  - src/lib/runtime/degradedDerivation.ts
  - src/lib/runtime/readModels.ts
  - src/adapters/catalog/catalogSnapshotLoader.ts
  - src/adapters/catalog/createIntegrationCatalogPort.ts
  - src/adapters/runtime/runtimeTransport.ts
  - src/adapters/runtime/createIntegrationRuntimePort.ts
  - src/adapters/unity/unityTransport.ts
  - src/adapters/unity/createIntegrationUnityPort.ts
autonomous: true
requirements:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Demo, integration, and degraded runtime seams are exercised by automated checks at the adapter, transport, orchestration, normalization, and read-model layers.
    - Failure and recovery behavior is explicitly proved: send failures, snapshot-load failures, unavailable inputs, stale events, and readiness recovery all remain local-first and calm.
    - Any code fix discovered during runtime hardening stays inside the approved runtime/adapters/lib boundaries and does not redesign ports, stores, or presentation.
  artifacts:
    - src/adapters/catalog/catalogSnapshotLoader.test.ts
    - src/adapters/catalog/createIntegrationCatalogPort.test.ts
    - src/adapters/runtime/runtimeTransport.test.ts
    - src/adapters/runtime/createIntegrationRuntimePort.test.ts
    - src/adapters/unity/unityTransport.test.ts
    - src/adapters/unity/createIntegrationUnityPort.test.ts
    - src/app/runtime/createAppRuntime.test.ts
    - src/app/runtime/runtimeLifecycle.test.ts
    - src/lib/runtime/normalization.test.ts
    - src/lib/runtime/staleEventGuard.test.ts
    - src/lib/runtime/degradedDerivation.test.ts
    - src/lib/runtime/readModels.test.ts
  key_links:
    - src/app/runtime/createAppRuntime.ts remains the single orchestrator for startup, teardown, reset sequencing, and adapter wiring.
    - src/lib/runtime/applyInboundEvent.ts remains the only event-to-store write boundary.
    - src/adapters/* integration factories and transport wrappers remain store-free seams.
---

<objective>
Add risk-based automated proof for the runtime and degraded seams so Phase 7 can certify runtime resilience, stale-event safety, and calm recovery behavior across demo and integration modes.

Purpose: Close the remaining proof gaps around transport boundaries, orchestrator failure handling, and degraded recovery without changing the approved runtime architecture.
Output: Focused adapter/transport/orchestrator/degraded tests, plus any minimal contained runtime fixes required by findings.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-verification-and-hardening/07-CONTEXT.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-01-SUMMARY.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-02-SUMMARY.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-03-SUMMARY.md
@src/app/runtime/createAppRuntime.ts
@src/app/runtime/createAppRuntime.test.ts
@src/app/runtime/runtimeLifecycle.test.ts
@src/lib/runtime/applyInboundEvent.ts
@src/lib/runtime/normalization.test.ts
@src/lib/runtime/staleEventGuard.test.ts
@src/lib/runtime/degradedDerivation.test.ts
@src/lib/runtime/readModels.test.ts
@src/adapters/catalog/catalogSnapshotLoader.ts
@src/adapters/catalog/createIntegrationCatalogPort.ts
@src/adapters/runtime/runtimeTransport.ts
@src/adapters/runtime/createIntegrationRuntimePort.ts
@src/adapters/unity/unityTransport.ts
@src/adapters/unity/createIntegrationUnityPort.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add direct tests for integration adapters and transport wrappers</name>
  <files>src/adapters/catalog/catalogSnapshotLoader.test.ts, src/adapters/catalog/createIntegrationCatalogPort.test.ts, src/adapters/runtime/runtimeTransport.test.ts, src/adapters/runtime/createIntegrationRuntimePort.test.ts, src/adapters/unity/unityTransport.test.ts, src/adapters/unity/createIntegrationUnityPort.test.ts, src/adapters/catalog/catalogSnapshotLoader.ts, src/adapters/catalog/createIntegrationCatalogPort.ts, src/adapters/runtime/runtimeTransport.ts, src/adapters/runtime/createIntegrationRuntimePort.ts, src/adapters/unity/unityTransport.ts, src/adapters/unity/createIntegrationUnityPort.ts</files>
  <read_first>src/adapters/catalog/catalogSnapshotLoader.ts, src/adapters/catalog/createIntegrationCatalogPort.ts, src/adapters/runtime/runtimeTransport.ts, src/adapters/runtime/createIntegrationRuntimePort.ts, src/adapters/unity/unityTransport.ts, src/adapters/unity/createIntegrationUnityPort.ts, src/adapters/contracts/ports.ts, src/adapters/contracts/runtime.ts, src/adapters/contracts/unity.ts, src/adapters/contracts/catalog.ts, .planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md</read_first>
  <action>Create focused integration-seam tests that verify these exact behaviors: `catalogSnapshotLoader` returns the normalized snapshot payload when the local loader succeeds and throws a controlled failure when the loader rejects or returns malformed data; `createIntegrationCatalogPort()` implements required `loadSnapshot()` and preserves optional `subscribe()` behavior without writing stores; `runtimeTransport` and `unityTransport` subscribe/unsubscribe correctly and normalize send/receive behavior through the existing contract surface; `createIntegrationRuntimePort()` and `createIntegrationUnityPort()` delegate only through the transport wrappers and emit the locked contract events. If a failing test exposes a seam bug, fix only the affected adapter or transport file and keep all adapter modules free of store or presentation imports.</action>
  <verify>npm run test -- --run src/adapters/catalog/catalogSnapshotLoader.test.ts src/adapters/catalog/createIntegrationCatalogPort.test.ts src/adapters/runtime/runtimeTransport.test.ts src/adapters/runtime/createIntegrationRuntimePort.test.ts src/adapters/unity/unityTransport.test.ts src/adapters/unity/createIntegrationUnityPort.test.ts</verify>
  <acceptance_criteria>
    - The six listed `*.test.ts` files exist and directly exercise integration adapter/transport behavior.
    - `src/adapters/catalog/createIntegrationCatalogPort.test.ts` asserts `loadSnapshot()` behavior and does not require store access.
    - `src/adapters/runtime/createIntegrationRuntimePort.test.ts` and `src/adapters/unity/createIntegrationUnityPort.test.ts` assert delegation through transport wrappers and contract-safe event emission.
    - No adapter source file imports from `@/stores`, `@/components`, or `@/app`.
  </acceptance_criteria>
  <done>The integration-mode seams that were previously only inferred from higher-level tests now have direct automated proof.</done>
</task>

<task type="auto">
  <name>Task 2: Expand runtime lifecycle hardening for send failures, restart safety, and stale-event rejection</name>
  <files>src/app/runtime/createAppRuntime.test.ts, src/app/runtime/runtimeLifecycle.test.ts, src/lib/runtime/staleEventGuard.test.ts, src/app/runtime/createAppRuntime.ts, src/lib/runtime/staleEventGuard.ts</files>
  <read_first>src/app/runtime/createAppRuntime.ts, src/app/runtime/createAppRuntime.test.ts, src/app/runtime/runtimeLifecycle.test.ts, src/lib/runtime/staleEventGuard.ts, src/lib/runtime/staleEventGuard.test.ts, src/lib/sessionReset/resetSession.ts, .planning/phases/07-verification-and-hardening/07-CONTEXT.md</read_first>
  <action>Extend the runtime tests so they prove the exact failure and lifecycle behavior Phase 7 needs: when `runtimePort.send()` fails for `shopper.session.start` or `shopper.session.end`, operational status is marked unavailable without breaking local reset authority; when `unityPort.send()` fails during garment/size/color selection, the runtime records the Unity surface failure and does not mutate shopper flow incorrectly; after `stop()` or `restart()`, old listeners cannot deliver stale session-scoped events into the stores; stale app-scoped events that are still allowed continue to work. If a failure reveals missing protection, fix only `src/app/runtime/createAppRuntime.ts` or `src/lib/runtime/staleEventGuard.ts`.</action>
  <verify>npm run test -- --run src/app/runtime/createAppRuntime.test.ts src/app/runtime/runtimeLifecycle.test.ts src/lib/runtime/staleEventGuard.test.ts</verify>
  <acceptance_criteria>
    - `src/app/runtime/runtimeLifecycle.test.ts` contains assertions for send-failure handling during session commands and selection commands.
    - `src/app/runtime/createAppRuntime.test.ts` contains assertions for stop/restart listener cleanup beyond simple idempotence.
    - `src/lib/runtime/staleEventGuard.test.ts` contains assertions for stale rejection after restart as well as after reset/session replacement.
    - No fix changes the `AppRuntime` public API or the port contract interfaces.
  </acceptance_criteria>
  <done>The runtime lifecycle is explicitly proved against the failure and stale-event cases most likely to break trust in a kiosk setting.</done>
</task>

<task type="auto">
  <name>Task 3: Add degraded and recovery proof for catalog, Unity, measurements, and fit</name>
  <files>src/lib/runtime/normalization.test.ts, src/lib/runtime/degradedDerivation.test.ts, src/lib/runtime/readModels.test.ts, src/lib/runtime/applyInboundEvent.ts, src/lib/runtime/normalizeCatalogEvent.ts, src/lib/runtime/normalizeRuntimeEvent.ts, src/lib/runtime/normalizeUnityEvent.ts, src/lib/runtime/degradedDerivation.ts, src/lib/runtime/readModels.ts</files>
  <read_first>src/lib/runtime/applyInboundEvent.ts, src/lib/runtime/normalizeCatalogEvent.ts, src/lib/runtime/normalizeRuntimeEvent.ts, src/lib/runtime/normalizeUnityEvent.ts, src/lib/runtime/degradedDerivation.ts, src/lib/runtime/readModels.ts, src/lib/runtime/normalization.test.ts, src/lib/runtime/degradedDerivation.test.ts, src/lib/runtime/readModels.test.ts, .planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md</read_first>
  <action>Extend the runtime mapping and read-model tests with explicit recovery cases: `catalog.snapshot.unavailable` followed by `catalog.snapshot.updated` clears catalog-unavailable degraded state while preserving valid selection behavior; Unity `delayed` or `unavailable` state returning to `ready` clears Unity blocking/attention issues correctly; `runtime.measurements.updated` and `runtime.fit.updated` can move from `partial` or `unavailable` back to `ready` without leaving stale shopper guidance behind; blocking detection/runtime issues suppress `readyToAdvance`, then release it again after recovery. If a test exposes a local defect, fix only the normalizer, derivation, or read-model file involved.</action>
  <verify>npm run test -- --run src/lib/runtime/normalization.test.ts src/lib/runtime/degradedDerivation.test.ts src/lib/runtime/readModels.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/runtime/normalization.test.ts` contains recovery-path assertions for catalog, runtime, and Unity event families.
    - `src/lib/runtime/degradedDerivation.test.ts` contains explicit clear-on-recovery assertions for catalog, Unity, measurements, and fit issue families.
    - `src/lib/runtime/readModels.test.ts` contains `readyToAdvance` assertions for both blocking and recovered states.
    - No source change introduces raw transport fields into read-model output or presentation-facing state.
  </acceptance_criteria>
  <done>The degraded layer is proved as a calm recoverable overlay rather than a one-way failure state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/adapters/catalog/catalogSnapshotLoader.test.ts src/adapters/catalog/createIntegrationCatalogPort.test.ts src/adapters/runtime/runtimeTransport.test.ts src/adapters/runtime/createIntegrationRuntimePort.test.ts src/adapters/unity/unityTransport.test.ts src/adapters/unity/createIntegrationUnityPort.test.ts src/app/runtime/createAppRuntime.test.ts src/app/runtime/runtimeLifecycle.test.ts src/lib/runtime/normalization.test.ts src/lib/runtime/staleEventGuard.test.ts src/lib/runtime/degradedDerivation.test.ts src/lib/runtime/readModels.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "describe\\('runtimeTransport'|describe\\('unityTransport'|describe\\('catalogSnapshotLoader'" src/adapters/**/*.test.ts`
- [ ] `rg -n "createAppRuntime|shouldApplyInboundEvent|readyToAdvance|catalog.snapshot.unavailable" src/app/runtime/*.ts src/lib/runtime/*.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No shell/store/selector architecture drift introduced
- Demo, integration, and degraded seams have direct automated proof for failure, recovery, and stale-event safety
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-and-hardening/07-02-SUMMARY.md`
</output>
