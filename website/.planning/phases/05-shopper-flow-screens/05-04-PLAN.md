---
phase: 05-shopper-flow-screens
plan: 04
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - src/features/shopper/session/usePostResetConfirmation.ts
  - src/features/shopper/session/ShopperPhaseRenderer.tsx
  - src/screens/shopper/index.ts
  - src/screens/shopper/SessionEndConfirmationScreen.tsx
  - src/screens/shopper/PostResetConfirmation.test.tsx
  - src/screens/shopper/ShopperFlow.test.tsx
autonomous: true
requirements:
  - SHOP-07
user_setup: []
must_haves:
  truths:
    - Post-reset confirmation is implemented as a UI-local ephemeral presentation layer outside shopper-scoped stores.
    - Immediate local reset stays authoritative; the confirmation surface appears after reset and never reuses cleared shopper measurements, garment selection, or fit data.
    - Phase-level interaction coverage proves the fixed shopper flow from `idle` through end-of-session behavior without adding routes, admin workflows, or runtime redesign.
  artifacts:
    - src/features/shopper/session/usePostResetConfirmation.ts
    - src/screens/shopper/SessionEndConfirmationScreen.tsx
    - src/screens/shopper/PostResetConfirmation.test.tsx
    - src/screens/shopper/ShopperFlow.test.tsx
  key_links:
    - `usePostResetConfirmation.ts` is the frozen UI-local seam that detects reset completion from existing session affordance selectors rather than store redesign.
    - `SessionEndConfirmationScreen.tsx` renders the exact approved confirmation copy for 2400ms and no shopper-scoped data.
    - `ShopperFlow.test.tsx` becomes the phase-level interaction suite for start, auto-advance detection, catalog selection, try-on, fit-details return, and end-session reset confirmation.
---

<objective>
Implement the post-reset confirmation surface and the final cross-phase shopper interaction coverage.

Purpose: Finish Phase 5 with immediate privacy-safe reset behavior that still gives the shopper visible deletion confirmation, and verify the complete shopper flow end to end.
Output: UI-local post-reset confirmation hook and screen module, updated phase renderer precedence, and full shopper-flow interaction tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-shopper-flow-screens/05-CONTEXT.md
@.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md
@.planning/phases/05-shopper-flow-screens/05-01-PLAN.md
@.planning/phases/05-shopper-flow-screens/05-02-PLAN.md
@.planning/phases/05-shopper-flow-screens/05-03-PLAN.md
@src/features/shopper/session/ShopperPhaseRenderer.tsx
@src/stores/session/selectors.ts
@src/lib/sessionReset/resetSession.ts
@src/app/runtime/createAppRuntime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the UI-local post-reset confirmation seam and screen module</name>
  <files>src/features/shopper/session/usePostResetConfirmation.ts, src/screens/shopper/SessionEndConfirmationScreen.tsx, src/screens/shopper/index.ts, src/features/shopper/session/ShopperPhaseRenderer.tsx</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/features/shopper/session/ShopperPhaseRenderer.tsx, src/stores/session/selectors.ts, src/lib/sessionReset/resetSession.ts</read_first>
  <action>Create `src/features/shopper/session/usePostResetConfirmation.ts` as a UI-local hook that watches the existing session affordance selectors and triggers a confirmation-visible state when `selectCanEndSession()` was previously `true`, the shopper flow has returned to idle, and `selectCanStartSession()` is `true`. Lock the visibility duration to exactly `2400` milliseconds through a local timer; do not write new store state. Create `src/screens/shopper/SessionEndConfirmationScreen.tsx` exporting `createSessionEndConfirmationLayout()` that renders the exact heading `Session ended.` and body `Measurements and garment selections were cleared on this device.` in the protected stage, uses subdued privacy summary content in the rail, clears both overlay lanes, and renders no shopper-scoped data. Update `src/features/shopper/session/ShopperPhaseRenderer.tsx` so the confirmation layout takes precedence over `idle` only while the local confirmation hook is active.</action>
  <verify>rg -n "2400|setTimeout|selectCanEndSession|selectCanStartSession" src/features/shopper/session/usePostResetConfirmation.ts && rg -n "Session ended\\.|Measurements and garment selections were cleared on this device\\.|createSessionEndConfirmationLayout" src/screens/shopper/SessionEndConfirmationScreen.tsx && rg -n "usePostResetConfirmation|createSessionEndConfirmationLayout" src/features/shopper/session/ShopperPhaseRenderer.tsx</verify>
  <acceptance_criteria>
    - `src/features/shopper/session/usePostResetConfirmation.ts` contains `2400` and both `selectCanEndSession` and `selectCanStartSession`.
    - `src/screens/shopper/SessionEndConfirmationScreen.tsx` contains the two exact confirmation strings from the action text.
    - `src/screens/shopper/SessionEndConfirmationScreen.tsx` does not import measurement, fit, or catalog selectors.
    - `src/features/shopper/session/ShopperPhaseRenderer.tsx` uses `usePostResetConfirmation` and gives the confirmation layout precedence over normal idle rendering.
  </acceptance_criteria>
  <done>The shopper flow now shows a short-lived deletion confirmation after immediate reset without weakening privacy or adding shopper-scoped persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated confirmation timing coverage with fake timers and no-data assertions</name>
  <files>src/screens/shopper/PostResetConfirmation.test.tsx</files>
  <read_first>src/features/shopper/session/usePostResetConfirmation.ts, src/screens/shopper/SessionEndConfirmationScreen.tsx, src/features/shopper/session/ShopperPhaseRenderer.tsx</read_first>
  <action>Create `src/screens/shopper/PostResetConfirmation.test.tsx` using fake timers to verify the confirmation surface appears after a simulated shopper reset, remains visible for exactly `2400ms`, and then yields back to the normal idle screen. Include assertions that the confirmation surface renders `Session ended.` and `Measurements and garment selections were cleared on this device.` while not rendering prior garment names, measurement labels, fit reasons, or size recommendations from the cleared session.</action>
  <verify>npm run test -- --run src/screens/shopper/PostResetConfirmation.test.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/PostResetConfirmation.test.tsx` contains a fake-timer test for `2400ms`.
    - `src/screens/shopper/PostResetConfirmation.test.tsx` asserts the two exact confirmation strings from the action text.
    - `src/screens/shopper/PostResetConfirmation.test.tsx` contains negative assertions for prior garment, measurement, or fit content.
    - `npm run test -- --run src/screens/shopper/PostResetConfirmation.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The post-reset confirmation behavior is pinned down with timing and privacy-boundary tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add full shopper-flow interaction tests for the fixed kiosk phase model</name>
  <files>src/screens/shopper/ShopperFlow.test.tsx</files>
  <read_first>src/screens/shopper/IdleScreen.tsx, src/screens/shopper/DetectionScreen.tsx, src/screens/shopper/CatalogScreen.tsx, src/screens/shopper/TryOnScreen.tsx, src/screens/shopper/FitDetailsScreen.tsx, src/screens/shopper/SessionEndConfirmationScreen.tsx, src/features/shopper/session/ShopperPhaseRenderer.tsx</read_first>
  <action>Create `src/screens/shopper/ShopperFlow.test.tsx` as the phase-level interaction suite. It must verify the exact fixed flow `idle -> detection -> catalog -> tryOn -> fitDetails -> sessionEnd`, with detection auto-advancing after seeded readiness, try-on entry requiring garment selection only, size/color variants remaining editable in try-on, fit-details returning to try-on through the rail takeover path, and end-session returning the app to idle plus the post-reset confirmation surface. Keep the suite selector-driven and local: seed stores and read models directly, do not simulate raw transport events in JSX tests, and do not mount admin mode.</action>
  <verify>npm run test -- --run src/screens/shopper/ShopperFlow.test.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/ShopperFlow.test.tsx` contains assertions for all six named shopper phases from the action text.
    - `src/screens/shopper/ShopperFlow.test.tsx` contains an assertion that try-on progression requires garment selection only.
    - `src/screens/shopper/ShopperFlow.test.tsx` contains an assertion that size/color remain editable inside `tryOn`.
    - `src/screens/shopper/ShopperFlow.test.tsx` does not import adapter transport wrappers or raw contract event modules.
    - `npm run test -- --run src/screens/shopper/ShopperFlow.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The entire Phase 5 shopper journey is covered by one phase-level interaction suite that enforces the fixed kiosk flow and local reset behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/screens/shopper/PostResetConfirmation.test.tsx src/screens/shopper/ShopperFlow.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "2400|Session ended\\.|Measurements and garment selections were cleared on this device\\." src/features/shopper/session/*.ts src/screens/shopper/*.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 ends with immediate local reset, a UI-local post-reset confirmation layer, and full shopper-flow interaction coverage without touching admin or runtime architecture
</success_criteria>

<output>
After completion, create `.planning/phases/05-shopper-flow-screens/05-04-SUMMARY.md`
</output>
