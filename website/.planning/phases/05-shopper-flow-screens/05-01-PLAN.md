---
phase: 05-shopper-flow-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/shell/ShopperShell.tsx
  - src/app/shell/AppShell.test.tsx
  - src/app/App.test.tsx
  - src/features/shopper/session/shopperPhaseLayout.ts
  - src/features/shopper/session/ShopperPhaseRenderer.tsx
  - src/features/shopper/common/ShopperBandHeader.tsx
  - src/features/shopper/common/StageHeroCard.tsx
  - src/features/shopper/common/NeutralStagePlaceholder.tsx
  - src/features/shopper/detection/DetectionReadinessChecklist.tsx
  - src/screens/shopper/index.ts
  - src/screens/shopper/IdleScreen.tsx
  - src/screens/shopper/DetectionScreen.tsx
  - src/screens/shopper/IdleDetectionScreen.test.tsx
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
user_setup: []
must_haves:
  truths:
    - The existing shopper shell remains the only shell frame, but its band, stage, rail, and overlay lanes are now fed by a Phase 5 screen-layout renderer instead of Phase 2 proof content.
    - `idle` and `detection` screens live under `src/screens/shopper/` and consume only approved selectors and runtime read models.
    - Detection remains auto-advance and uses the protected stage with runtime-backed readiness when available and a neutral branded placeholder when not.
  artifacts:
    - src/features/shopper/session/shopperPhaseLayout.ts
    - src/features/shopper/session/ShopperPhaseRenderer.tsx
    - src/features/shopper/common/ShopperBandHeader.tsx
    - src/features/shopper/common/StageHeroCard.tsx
    - src/features/shopper/common/NeutralStagePlaceholder.tsx
    - src/features/shopper/detection/DetectionReadinessChecklist.tsx
    - src/screens/shopper/IdleScreen.tsx
    - src/screens/shopper/DetectionScreen.tsx
    - src/screens/shopper/IdleDetectionScreen.test.tsx
  key_links:
    - `src/features/shopper/session/shopperPhaseLayout.ts` locks the slot contract used to map each shopper phase into the existing shell zones without redesigning `ShopperShell`.
    - `src/features/shopper/session/ShopperPhaseRenderer.tsx` is the only screen-selection seam; `ShopperShell` stays a shell composer rather than becoming a feature-state switchboard.
    - `IdleScreen.tsx` and `DetectionScreen.tsx` must consume selectors/read models only and must not import adapters, contract events, or runtime transports.
---

<objective>
Implement the Phase 5 shopper shell host boundary plus the real `idle` and `detection` screens.

Purpose: Replace Phase 2 shopper proof content with the first real shopper phases while preserving the locked shell composition, selector boundaries, and auto-advance detection flow.
Output: A reusable shopper phase-layout contract, a shell-level phase renderer, idle/detection screen modules, and focused shell/screen tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-shopper-flow-screens/05-CONTEXT.md
@.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md
@.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@src/app/shell/ShopperShell.tsx
@src/app/shell/AppShell.test.tsx
@src/app/App.test.tsx
@src/lib/runtime/readModels.ts
@src/stores/session/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shopper phase-layout and rendering seam without changing shell geometry</name>
  <files>src/features/shopper/session/shopperPhaseLayout.ts, src/features/shopper/session/ShopperPhaseRenderer.tsx, src/app/shell/ShopperShell.tsx, src/screens/shopper/index.ts</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/app/shell/ShopperShell.tsx, src/stores/session/selectors.ts, src/lib/runtime/readModels.ts</read_first>
  <action>Create `src/features/shopper/session/shopperPhaseLayout.ts` exporting a concrete `ShopperPhaseLayout` type with the exact fields `band`, `stage`, `rail`, `overlayTop`, and `overlayBottom`, each typed as `ReactNode | null` except the first three which are required. Create `src/features/shopper/session/ShopperPhaseRenderer.tsx` as the only shopper-phase selection seam. It must read `selectShopperPhase()` from the session store, route `idle` to `createIdleScreenLayout()` and `detection` to `createDetectionScreenLayout()`, and return a `ShopperPhaseLayout`. Update `src/app/shell/ShopperShell.tsx` so it preserves the existing `ShellFrame`, `ShellBand`, `StageViewport`, `ContextRail`, and `OverlayLane` structure but renders the returned layout nodes instead of `shopperProofContent`. Create `src/screens/shopper/index.ts` exporting the screen layout creators. Do not import from `@/adapters`, `@/mocks`, or `@/adapters/contracts` in these files.</action>
  <verify>rg -n "export type ShopperPhaseLayout|band: ReactNode|overlayTop|overlayBottom|createIdleScreenLayout|createDetectionScreenLayout" src/features/shopper/session/shopperPhaseLayout.ts src/features/shopper/session/ShopperPhaseRenderer.tsx src/screens/shopper/index.ts && rg -n "ShopperPhaseRenderer|layout\\.band|layout\\.stage|layout\\.rail" src/app/shell/ShopperShell.tsx</verify>
  <acceptance_criteria>
    - `src/features/shopper/session/shopperPhaseLayout.ts` contains `export type ShopperPhaseLayout`.
    - `src/features/shopper/session/ShopperPhaseRenderer.tsx` contains `selectShopperPhase` and routes `idle` and `detection`.
    - `src/app/shell/ShopperShell.tsx` still contains `ShellBand`, `StageViewport`, `ContextRail`, and two `OverlayLane` usages.
    - None of the files listed in `<files>` import from `@/adapters`, `@/adapters/contracts`, or transport wrappers.
  </acceptance_criteria>
  <done>The shopper shell is ready to host real shopper-phase content through a stable slot contract without redesigning the shell frame.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the real idle and detection screen modules plus reusable shopper chunks</name>
  <files>src/features/shopper/common/ShopperBandHeader.tsx, src/features/shopper/common/StageHeroCard.tsx, src/features/shopper/common/NeutralStagePlaceholder.tsx, src/features/shopper/detection/DetectionReadinessChecklist.tsx, src/screens/shopper/IdleScreen.tsx, src/screens/shopper/DetectionScreen.tsx</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/components/primitives/index.ts, src/lib/runtime/readModels.ts, src/stores/session/selectors.ts</read_first>
  <action>Create `src/features/shopper/common/ShopperBandHeader.tsx` for the shopper top-band content row with brand, phase label/support copy, and an optional action slot. Create `src/features/shopper/common/StageHeroCard.tsx` for the stage-safe heading/body/CTA surface, and `src/features/shopper/common/NeutralStagePlaceholder.tsx` for the locked idle/detection fallback host copy when no runtime-backed live impression is available. Create `src/features/shopper/detection/DetectionReadinessChecklist.tsx` that renders the exact three checklist lines `Step into view`, `Square your shoulders to the display`, and `Hold position while detection settles.`. Implement `src/screens/shopper/IdleScreen.tsx` exporting `createIdleScreenLayout()` that consumes `selectCanStartSession()` and `readDegradedState()`, renders the `Start Session` CTA in the stage-safe hero, keeps the right rail focused on privacy/input summary, and never renders admin/runtime internals. Implement `src/screens/shopper/DetectionScreen.tsx` exporting `createDetectionScreenLayout()` that consumes `selectCanEndSession()`, `readDetectionReadiness()`, and `readDegradedState()`, renders no continue button, uses the existing protected stage with readiness framing when available, and otherwise falls back to `NeutralStagePlaceholder` plus a `StageHeroCard` with shopper-safe guidance.</action>
  <verify>rg -n "Start Session|readDegradedState|selectCanStartSession|createIdleScreenLayout" src/screens/shopper/IdleScreen.tsx && rg -n "readDetectionReadiness|selectCanEndSession|createDetectionScreenLayout|Step into view|Square your shoulders to the display|Hold position while detection settles" src/screens/shopper/DetectionScreen.tsx src/features/shopper/detection/DetectionReadinessChecklist.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/IdleScreen.tsx` contains `Start Session` and `selectCanStartSession`.
    - `src/screens/shopper/DetectionScreen.tsx` contains `readDetectionReadiness` and does not contain `Continue`.
    - `src/features/shopper/common/NeutralStagePlaceholder.tsx` contains calm branded fallback copy and does not mention camera ownership, WebSocket, Unity internals, or transport terms.
    - `src/features/shopper/detection/DetectionReadinessChecklist.tsx` contains the three exact checklist strings from the action text.
  </acceptance_criteria>
  <done>The first two shopper phases are real UI and follow the locked shell-zone, copy, and selector boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Update shell tests and add focused idle/detection interaction coverage</name>
  <files>src/app/shell/AppShell.test.tsx, src/app/App.test.tsx, src/screens/shopper/IdleDetectionScreen.test.tsx</files>
  <read_first>src/app/shell/AppShell.test.tsx, src/app/App.test.tsx, src/screens/shopper/IdleScreen.tsx, src/screens/shopper/DetectionScreen.tsx</read_first>
  <action>Update `src/app/shell/AppShell.test.tsx` so shopper assertions remain about shell landmarks and shopper-zone composition only, and remove the outdated requirement that shopper proof copy contain `Stage Reserved`. Update `src/app/App.test.tsx` so the shopper-mode assertions look for the real idle CTA `Start Session` rather than the Phase 2 placeholder heading. Create `src/screens/shopper/IdleDetectionScreen.test.tsx` covering three flows: idle renders `Start Session` in shopper mode, detection renders no continue button after `startSession('keyboard')`, and detection guidance changes according to a seeded `readDetectionReadiness()` state without exposing transport terms. Keep admin-shell tests intact.</action>
  <verify>npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/screens/shopper/IdleDetectionScreen.test.tsx</verify>
  <acceptance_criteria>
    - `src/app/App.test.tsx` contains `Start Session`.
    - `src/app/shell/AppShell.test.tsx` no longer asserts shopper copy contains `Stage Reserved`.
    - `src/screens/shopper/IdleDetectionScreen.test.tsx` contains assertions for `Start Session`, absence of `Continue`, and detection guidance rendering.
    - `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/screens/shopper/IdleDetectionScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The shell and the first two shopper phases are covered by focused interaction tests without reintroducing placeholder-boundary assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/screens/shopper/IdleDetectionScreen.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "export type ShopperPhaseLayout|createIdleScreenLayout|createDetectionScreenLayout" src/features/shopper/session/*.ts* src/screens/shopper/*.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shopper shell now renders real `idle` and `detection` content through a stable screen-layout seam while preserving auto-advance detection and shell composition
</success_criteria>

<output>
After completion, create `.planning/phases/05-shopper-flow-screens/05-01-SUMMARY.md`
</output>
