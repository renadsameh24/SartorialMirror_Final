---
phase: 05-shopper-flow-screens
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/features/shopper/session/ShopperPhaseRenderer.tsx
  - src/features/shopper/tryOn/TryOnActionGroup.tsx
  - src/features/shopper/tryOn/TryOnStageOverlays.tsx
  - src/features/shopper/fit/MeasurementPanel.tsx
  - src/features/shopper/fit/FitSummaryCard.tsx
  - src/features/shopper/fit/FitDetailsRail.tsx
  - src/screens/shopper/index.ts
  - src/screens/shopper/TryOnScreen.tsx
  - src/screens/shopper/FitDetailsScreen.tsx
  - src/screens/shopper/TryOnScreen.test.tsx
  - src/screens/shopper/FitDetailsScreen.test.tsx
autonomous: true
requirements:
  - SHOP-04
  - SHOP-05
  - SHOP-06
user_setup: []
must_haves:
  truths:
    - `tryOn` keeps Unity visually central and pushes measurement, fit, and variant editing into the right rail and edge overlays.
    - `fitDetails` is implemented as a right-rail takeover / expanded rail mode rather than a new route or shell frame.
    - Measurement and fit presentation uses existing selectors/read models only and never exposes raw confidence scores or transport/event detail.
  artifacts:
    - src/features/shopper/tryOn/TryOnActionGroup.tsx
    - src/features/shopper/tryOn/TryOnStageOverlays.tsx
    - src/features/shopper/fit/MeasurementPanel.tsx
    - src/features/shopper/fit/FitSummaryCard.tsx
    - src/features/shopper/fit/FitDetailsRail.tsx
    - src/screens/shopper/TryOnScreen.tsx
    - src/screens/shopper/FitDetailsScreen.tsx
    - src/screens/shopper/TryOnScreen.test.tsx
    - src/screens/shopper/FitDetailsScreen.test.tsx
  key_links:
    - `TryOnScreen.tsx` must consume `selectActiveSelection`, `selectDisplayMeasurements`, `selectCurrentRecommendation`, `readUnityRenderReadiness`, `readMeasurementReadiness`, `readFitReadiness`, and `readDegradedState`.
    - `FitDetailsScreen.tsx` must reuse the Unity-centered stage and replace only the rail with `FitDetailsRail`.
    - `VariantSelector.tsx` from Plan 05-02 is the shared variant editing control in both `catalog` and `tryOn`.
---

<objective>
Implement the real `tryOn` and `fitDetails` shopper screens plus the measurement, fit, and overlay modules they depend on.

Purpose: Deliver the shopper’s main value moment while preserving Unity as the visual hero and keeping deeper fit explanation in the right rail.
Output: Try-on and fit-details screen modules, reusable measurement/fit UI modules, stage overlays, and focused interaction tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-shopper-flow-screens/05-CONTEXT.md
@.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md
@.planning/phases/05-shopper-flow-screens/05-02-PLAN.md
@src/features/shopper/common/VariantSelector.tsx
@src/features/shopper/session/ShopperPhaseRenderer.tsx
@src/stores/catalog/selectors.ts
@src/stores/measurements/selectors.ts
@src/stores/fit/selectors.ts
@src/lib/runtime/readModels.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the try-on rail modules and edge overlays without crowding the protected stage</name>
  <files>src/features/shopper/tryOn/TryOnActionGroup.tsx, src/features/shopper/tryOn/TryOnStageOverlays.tsx, src/features/shopper/fit/MeasurementPanel.tsx, src/features/shopper/fit/FitSummaryCard.tsx, src/screens/shopper/TryOnScreen.tsx</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/features/shopper/common/VariantSelector.tsx, src/stores/catalog/selectors.ts, src/stores/measurements/selectors.ts, src/stores/fit/selectors.ts, src/lib/runtime/readModels.ts</read_first>
  <action>Create `src/features/shopper/fit/MeasurementPanel.tsx` to render only `selectDisplayMeasurements()` output, never raw nullable measurement samples. Create `src/features/shopper/fit/FitSummaryCard.tsx` to render `selectFitSummary()`, `selectCurrentRecommendation()`, and `selectAlternativeSize()` without exposing `confidenceScore`. Create `src/features/shopper/tryOn/TryOnActionGroup.tsx` for the exact actions `Fit Details`, `Back to Catalog`, and `End Session`. Create `src/features/shopper/tryOn/TryOnStageOverlays.tsx` so the top overlay shows garment name plus selected size and the bottom overlay shows compact render/fit context only. Implement `src/screens/shopper/TryOnScreen.tsx` exporting `createTryOnScreenLayout()` that keeps the stage visually clear, places `VariantSelector`, `MeasurementPanel`, `FitSummaryCard`, and `TryOnActionGroup` in the right rail, and consumes only the approved selectors/read models named in the `key_links` section above.</action>
  <verify>rg -n "selectDisplayMeasurements|selectCurrentRecommendation|selectFitSummary|selectAlternativeSize|readUnityRenderReadiness|readMeasurementReadiness|readFitReadiness|readDegradedState|createTryOnScreenLayout" src/screens/shopper/TryOnScreen.tsx && rg -n "Fit Details|Back to Catalog|End Session" src/features/shopper/tryOn/TryOnActionGroup.tsx && ! rg -n "confidenceScore" src/features/shopper/fit/*.tsx src/screens/shopper/TryOnScreen.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/TryOnScreen.tsx` contains every selector/read-model name listed in the action text.
    - `src/features/shopper/tryOn/TryOnActionGroup.tsx` contains the three exact action labels from the action text.
    - `src/features/shopper/fit/MeasurementPanel.tsx` renders only display-safe measurements and does not import adapter or contract files.
    - No file in `<files>` contains `confidenceScore`.
  </acceptance_criteria>
  <done>The `tryOn` phase delivers a real Unity-centered shopper view with rail-based fit and measurement information and edge-safe overlays only.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the fit-details rail takeover without introducing a new route or shell frame</name>
  <files>src/features/shopper/fit/FitDetailsRail.tsx, src/features/shopper/session/ShopperPhaseRenderer.tsx, src/screens/shopper/FitDetailsScreen.tsx, src/screens/shopper/index.ts</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/screens/shopper/TryOnScreen.tsx, src/stores/fit/selectors.ts, src/stores/measurements/selectors.ts, src/lib/runtime/readModels.ts</read_first>
  <action>Create `src/features/shopper/fit/FitDetailsRail.tsx` with the exact content order `fit summary headline`, `plain-language explanation`, `reasons list`, `recommended size and current size comparison`, optional `Apply Recommended Size` action, optional alternative garment section when `alternativeGarmentId` resolves to a current catalog garment, and `Back to Try-On` action. Implement `src/screens/shopper/FitDetailsScreen.tsx` exporting `createFitDetailsScreenLayout()` that reuses the Unity-centered stage from `tryOn`, swaps only the right rail to `FitDetailsRail`, keeps overlays minimal, and consumes `selectCurrentRecommendation()`, `selectFitSummary()`, `selectAlternativeSize()`, `selectDisplayMeasurements()`, `readMeasurementReadiness()`, `readFitReadiness()`, `readUnityRenderReadiness()`, and `readDegradedState()`. Extend `src/features/shopper/session/ShopperPhaseRenderer.tsx` and `src/screens/shopper/index.ts` so `fitDetails` resolves to `createFitDetailsScreenLayout()`.</action>
  <verify>rg -n "createFitDetailsScreenLayout|selectCurrentRecommendation|selectFitSummary|selectAlternativeSize|selectDisplayMeasurements|readMeasurementReadiness|readFitReadiness|readUnityRenderReadiness|readDegradedState" src/screens/shopper/FitDetailsScreen.tsx && rg -n "Apply Recommended Size|Back to Try-On|alternativeGarmentId" src/features/shopper/fit/FitDetailsRail.tsx && rg -n "fitDetails" src/features/shopper/session/ShopperPhaseRenderer.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/FitDetailsScreen.tsx` contains every selector/read-model name listed in the action text.
    - `src/features/shopper/fit/FitDetailsRail.tsx` contains `Apply Recommended Size` and `Back to Try-On`.
    - `src/features/shopper/session/ShopperPhaseRenderer.tsx` routes `fitDetails` to `createFitDetailsScreenLayout`.
    - `src/screens/shopper/FitDetailsScreen.tsx` does not create a new `ShellFrame`, `StageViewport`, or route component.
  </acceptance_criteria>
  <done>The `fitDetails` phase is a real right-rail takeover built inside the same shopper shell and Unity stage context.</done>
</task>

<task type="auto">
  <name>Task 3: Add try-on and fit-details interaction tests with variant editing and degraded-state coverage</name>
  <files>src/screens/shopper/TryOnScreen.test.tsx, src/screens/shopper/FitDetailsScreen.test.tsx</files>
  <read_first>src/screens/shopper/TryOnScreen.tsx, src/screens/shopper/FitDetailsScreen.tsx, src/features/shopper/common/VariantSelector.tsx, src/features/shopper/fit/FitDetailsRail.tsx</read_first>
  <action>Create `src/screens/shopper/TryOnScreen.test.tsx` covering variant controls rendering in the rail, measurement and fit modules rendering from seeded selector data, and degraded render states showing compact shopper-safe fallback messaging without removing the Unity-centered stage shell. Create `src/screens/shopper/FitDetailsScreen.test.tsx` covering right-rail takeover rendering, `Back to Try-On`, `Apply Recommended Size` visibility when `alternativeSize` exists, and omission of the alternative garment section when no matching garment resolves from the current catalog snapshot.</action>
  <verify>npm run test -- --run src/screens/shopper/TryOnScreen.test.tsx src/screens/shopper/FitDetailsScreen.test.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/TryOnScreen.test.tsx` contains assertions for variant editing, measurement rendering, fit rendering, and degraded fallback messaging.
    - `src/screens/shopper/FitDetailsScreen.test.tsx` contains assertions for rail takeover, `Back to Try-On`, conditional `Apply Recommended Size`, and conditional alternative garment omission.
    - `npm run test -- --run src/screens/shopper/TryOnScreen.test.tsx src/screens/shopper/FitDetailsScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The try-on and fit-details phases are covered by focused interaction tests that enforce the Unity-hero layout and rail takeover rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/screens/shopper/TryOnScreen.test.tsx src/screens/shopper/FitDetailsScreen.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "createTryOnScreenLayout|createFitDetailsScreenLayout|Fit Details|Back to Catalog|Back to Try-On" src/screens/shopper/*.tsx src/features/shopper/**/*.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shopper flow now includes real `tryOn` and `fitDetails` phases with Unity-centered stage treatment, rail-based variant editing, and plain-language fit explanation
</success_criteria>

<output>
After completion, create `.planning/phases/05-shopper-flow-screens/05-03-SUMMARY.md`
</output>
