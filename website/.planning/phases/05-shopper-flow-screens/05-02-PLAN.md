---
phase: 05-shopper-flow-screens
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/features/shopper/session/ShopperPhaseRenderer.tsx
  - src/features/shopper/common/VariantSelector.tsx
  - src/features/shopper/catalog/CatalogCategoryTabs.tsx
  - src/features/shopper/catalog/GarmentCard.tsx
  - src/features/shopper/catalog/GarmentGrid.tsx
  - src/features/shopper/catalog/CatalogSelectionRail.tsx
  - src/screens/shopper/index.ts
  - src/screens/shopper/CatalogScreen.tsx
  - src/screens/shopper/CatalogScreen.test.tsx
autonomous: true
requirements:
  - SHOP-03
  - SHOP-08
user_setup: []
must_haves:
  truths:
    - The catalog phase is implemented as a real stage-first shopper screen using the existing shell zones rather than a route or a dashboard sublayout.
    - Garment browsing, category selection, and selection readiness are driven only by approved catalog selectors/read models and existing catalog store actions.
    - The catalog rail holds the selected-garment summary, optional size/color controls, and the `Try On` action, while the protected stage stays focused on browsing.
  artifacts:
    - src/features/shopper/common/VariantSelector.tsx
    - src/features/shopper/catalog/CatalogCategoryTabs.tsx
    - src/features/shopper/catalog/GarmentCard.tsx
    - src/features/shopper/catalog/GarmentGrid.tsx
    - src/features/shopper/catalog/CatalogSelectionRail.tsx
    - src/screens/shopper/CatalogScreen.tsx
    - src/screens/shopper/CatalogScreen.test.tsx
  key_links:
    - `CatalogScreen.tsx` must consume `selectVisibleCategories`, `selectVisibleGarments`, `selectActiveSelection`, `selectSelectionReadyForTryOn`, `readCatalogReadiness`, and `readDegradedState`.
    - `VariantSelector.tsx` is the reusable size/color control seam that later `tryOn` reuses rather than inventing a second variant UI.
    - `ShopperPhaseRenderer.tsx` is extended to route `catalog` without adding a new shell frame or new shopper route.
---

<objective>
Implement the real `catalog` shopper screen and its reusable catalog selection modules.

Purpose: Move the shopper from detection into a curated local catalog experience that preserves stage-first browsing, right-rail context, and selector-driven state.
Output: Catalog screen module, category and garment-selection components, reusable variant selector, and focused catalog interaction tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-shopper-flow-screens/05-CONTEXT.md
@.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md
@.planning/phases/05-shopper-flow-screens/05-01-PLAN.md
@src/features/shopper/session/ShopperPhaseRenderer.tsx
@src/stores/catalog/selectors.ts
@src/stores/catalog/catalogStore.ts
@src/lib/runtime/readModels.ts
@src/mocks/catalog/catalogFixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the catalog stage modules for category filtering and garment browsing</name>
  <files>src/features/shopper/catalog/CatalogCategoryTabs.tsx, src/features/shopper/catalog/GarmentCard.tsx, src/features/shopper/catalog/GarmentGrid.tsx, src/screens/shopper/CatalogScreen.tsx</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/stores/catalog/selectors.ts, src/stores/catalog/catalogStore.ts, src/components/primitives/index.ts, src/mocks/catalog/catalogFixtures.ts</read_first>
  <action>Create `src/features/shopper/catalog/CatalogCategoryTabs.tsx` as the keyboard- and mouse-operable category control strip for the protected stage. Create `src/features/shopper/catalog/GarmentCard.tsx` rendering the exact catalog-safe fields `name`, `category label`, and available variant context only; it must not render price, reviews, inventory count, or commerce badges. Create `src/features/shopper/catalog/GarmentGrid.tsx` to lay out the visible garments from `selectVisibleGarments()` in a restrained stage-safe grid. Implement `src/screens/shopper/CatalogScreen.tsx` exporting `createCatalogScreenLayout()` that consumes `selectCatalogStatus()`, `selectVisibleCategories()`, `selectVisibleGarments()`, `selectActiveSelection()`, `selectSelectionReadyForTryOn()`, `readCatalogReadiness()`, and `readDegradedState()`, renders category tabs plus garment grid in the protected stage, and never reads adapter events or transport status directly.</action>
  <verify>rg -n "selectVisibleCategories|selectVisibleGarments|selectActiveSelection|selectSelectionReadyForTryOn|readCatalogReadiness|createCatalogScreenLayout" src/screens/shopper/CatalogScreen.tsx && rg -n "price|review|inventory" src/features/shopper/catalog/GarmentCard.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/CatalogScreen.tsx` contains every selector/read-model name listed in the action text.
    - `src/features/shopper/catalog/GarmentCard.tsx` does not contain `price`, `review`, `inventory`, `checkout`, or `cart`.
    - `src/features/shopper/catalog/GarmentGrid.tsx` renders visible garments only and does not import `@/adapters` or runtime contracts.
  </acceptance_criteria>
  <done>The protected stage now hosts a real curated catalog browse experience built only on approved catalog selectors and local stage-safe components.</done>
</task>

<task type="auto">
  <name>Task 2: Build the catalog rail, reusable variant selector, and catalog phase routing</name>
  <files>src/features/shopper/common/VariantSelector.tsx, src/features/shopper/catalog/CatalogSelectionRail.tsx, src/features/shopper/session/ShopperPhaseRenderer.tsx, src/screens/shopper/index.ts, src/screens/shopper/CatalogScreen.tsx</files>
  <read_first>.planning/phases/05-shopper-flow-screens/05-UI-SPEC.md, src/features/shopper/session/ShopperPhaseRenderer.tsx, src/stores/catalog/catalogStore.ts, src/stores/catalog/selectors.ts, src/stores/session/sessionStore.ts</read_first>
  <action>Create `src/features/shopper/common/VariantSelector.tsx` with the exact props `label`, `options`, `value`, and `onChange`, supporting both size and color option rendering through existing catalog entities. Create `src/features/shopper/catalog/CatalogSelectionRail.tsx` for the right-rail selected-garment summary, optional size/color controls, and primary `Try On` action. Implement the locked behavior that `Try On` is enabled when `selectSelectionReadyForTryOn()` is true and that size/color controls appear when available but are not required for catalog-to-tryOn transition. Update `CatalogScreen.tsx` to place the rail component in the `rail` slot and use `VariantSelector` for optional size/color editing. Extend `src/features/shopper/session/ShopperPhaseRenderer.tsx` and `src/screens/shopper/index.ts` so `catalog` resolves to `createCatalogScreenLayout()`.</action>
  <verify>rg -n "export function VariantSelector|label|options|value|onChange" src/features/shopper/common/VariantSelector.tsx && rg -n "Try On|selectSelectionReadyForTryOn|VariantSelector" src/features/shopper/catalog/CatalogSelectionRail.tsx src/screens/shopper/CatalogScreen.tsx && rg -n "catalog" src/features/shopper/session/ShopperPhaseRenderer.tsx</verify>
  <acceptance_criteria>
    - `src/features/shopper/common/VariantSelector.tsx` exports `VariantSelector` and includes the four exact prop names from the action text.
    - `src/features/shopper/catalog/CatalogSelectionRail.tsx` contains `Try On` and `selectSelectionReadyForTryOn`.
    - `src/features/shopper/session/ShopperPhaseRenderer.tsx` routes `catalog` to `createCatalogScreenLayout`.
    - The catalog rail does not require size/color before `Try On` becomes available when a garment is already selected.
  </acceptance_criteria>
  <done>The catalog phase now fills both the protected stage and right rail, and its reusable variant selector is ready for later try-on reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Add catalog interaction coverage and enforce the no-commerce boundary</name>
  <files>src/screens/shopper/CatalogScreen.test.tsx</files>
  <read_first>src/screens/shopper/CatalogScreen.tsx, src/features/shopper/catalog/CatalogSelectionRail.tsx, src/features/shopper/common/VariantSelector.tsx, src/mocks/catalog/catalogFixtures.ts</read_first>
  <action>Create `src/screens/shopper/CatalogScreen.test.tsx` covering four cases: visible categories and garments render from seeded catalog store data, selecting a garment enables `Try On`, optional size/color controls render when available without blocking garment-only progression, and unavailable catalog readiness disables `Try On` while keeping the stage in a calm unavailable state. Include one boundary assertion that no catalog surface renders price, reviews, inventory, or checkout language.</action>
  <verify>npm run test -- --run src/screens/shopper/CatalogScreen.test.tsx</verify>
  <acceptance_criteria>
    - `src/screens/shopper/CatalogScreen.test.tsx` contains assertions for garment render, `Try On` enablement, optional variant controls, and unavailable catalog behavior.
    - `src/screens/shopper/CatalogScreen.test.tsx` contains a negative assertion for `price`, `review`, `inventory`, or `checkout`.
    - `npm run test -- --run src/screens/shopper/CatalogScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The catalog screen is covered by focused interaction tests and the shopper-safe non-commerce boundary is enforced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/screens/shopper/CatalogScreen.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "createCatalogScreenLayout|VariantSelector|Try On" src/screens/shopper/CatalogScreen.tsx src/features/shopper/**/*.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The catalog phase is a real shopper screen with stage-first browsing, rail-based selection context, optional variant controls, and no commerce leakage
</success_criteria>

<output>
After completion, create `.planning/phases/05-shopper-flow-screens/05-02-SUMMARY.md`
</output>
