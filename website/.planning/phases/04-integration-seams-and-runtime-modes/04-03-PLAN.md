---
phase: 04-integration-seams-and-runtime-modes
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/types/system.ts
  - src/stores/systemHealth/systemHealthStore.ts
  - src/stores/systemHealth/selectors.ts
  - src/stores/session/selectors.ts
  - src/stores/catalog/selectors.ts
  - src/stores/measurements/selectors.ts
  - src/stores/fit/selectors.ts
  - src/stores/degraded/selectors.ts
  - src/lib/runtime/degradedDerivation.ts
  - src/lib/runtime/readModels.ts
  - src/app/runtime/createAppRuntime.ts
  - src/lib/runtime/degradedDerivation.test.ts
  - src/lib/runtime/readModels.test.ts
  - src/app/runtime/runtimeLifecycle.test.ts
  - src/lib/sessionReset/resetSession.test.ts
autonomous: true
requirements:
  - MODE-03
  - MODE-04
user_setup: []
must_haves:
  truths:
    - `systemHealth` remains the owner of operational status through a small normalized helper shape alongside the enriched health snapshots already defined in Phase 3.
    - `readyToAdvance` is implemented as a derived selector/read model only, and is true only when the required readiness inputs are satisfied and no blocking degraded issue exists.
    - The app exposes shopper-safe read models for detection readiness, catalog readiness, Unity render readiness, measurement readiness, fit readiness, and degraded severity/blocking state without leaking raw transport payloads into presentation.
  artifacts:
    - src/types/system.ts
    - src/stores/systemHealth/systemHealthStore.ts
    - src/stores/systemHealth/selectors.ts
    - src/stores/session/selectors.ts
    - src/stores/catalog/selectors.ts
    - src/stores/measurements/selectors.ts
    - src/stores/fit/selectors.ts
    - src/stores/degraded/selectors.ts
    - src/lib/runtime/degradedDerivation.ts
    - src/lib/runtime/readModels.ts
    - src/app/runtime/createAppRuntime.ts
    - src/lib/runtime/degradedDerivation.test.ts
    - src/lib/runtime/readModels.test.ts
    - src/app/runtime/runtimeLifecycle.test.ts
    - src/lib/sessionReset/resetSession.test.ts
  key_links:
    - src/lib/runtime/degradedDerivation.ts turns normalized operational/runtime inputs into shopper-safe degraded issues and guidance only.
    - src/lib/runtime/readModels.ts becomes the cross-domain runtime-safe read surface for shopper readiness and blocking state.
    - src/app/runtime/createAppRuntime.ts finalizes teardown/reset interaction points so local reset stays authoritative and late events are ignored.
---

<objective>
Implement degraded derivation, runtime-safe read models, and teardown/reset lifecycle hardening on top of the runtime pipeline.

Purpose: Finish Phase 4 with shopper-safe readiness surfaces and calm degraded behavior without adding shopper screens or admin workflows.
Output: Operational-status helper shape, degraded derivation, read-model additions, lifecycle/reset hardening, and focused tests for degraded behavior and runtime lifecycle correctness.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-01-PLAN.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-02-PLAN.md
@src/types/system.ts
@src/stores/systemHealth/systemHealthStore.ts
@src/stores/systemHealth/selectors.ts
@src/stores/session/selectors.ts
@src/stores/catalog/selectors.ts
@src/stores/measurements/selectors.ts
@src/stores/fit/selectors.ts
@src/stores/degraded/selectors.ts
@src/lib/sessionReset/resetSession.ts
@src/lib/sessionReset/resetSession.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend operational status ownership and implement degraded derivation</name>
  <files>src/types/system.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/systemHealth/selectors.ts, src/lib/runtime/degradedDerivation.ts</files>
  <read_first>.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md, src/types/system.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/systemHealth/selectors.ts</read_first>
  <action>Extend the system-health domain just enough to let `systemHealth` remain the owner of operational surface status via a small normalized helper shape alongside the existing enriched `HealthSignal[]` snapshots. Implement `degradedDerivation.ts` to translate normalized operational/runtime inputs into the locked degraded issue taxonomy and shopper-safe guidance. Keep degraded derived rather than a peer app mode, and do not move ownership of guidance/issues out of the existing `degraded` store.</action>
  <verify>rg -n "camera|runtime|unity|catalog|blocking|degraded|attention|offline|healthy" src/types/system.ts src/stores/systemHealth/*.ts src/lib/runtime/degradedDerivation.ts</verify>
  <acceptance_criteria>
    - `systemHealth` still owns operational status and no new global runtime store is introduced.
    - `degradedDerivation.ts` encodes the locked issue families for detection, runtime, catalog, Unity, measurements, and fit.
    - No shopper-facing degraded copy exposes stack traces, raw confidence numbers, or transport jargon.
  </acceptance_criteria>
  <done>The app has an explicit degraded-derivation layer rooted in normalized operational state without changing store ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime-safe selectors and read models, including derived readyToAdvance</name>
  <files>src/stores/session/selectors.ts, src/stores/catalog/selectors.ts, src/stores/measurements/selectors.ts, src/stores/fit/selectors.ts, src/stores/degraded/selectors.ts, src/lib/runtime/readModels.ts</files>
  <read_first>.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md, src/stores/session/selectors.ts, src/stores/catalog/selectors.ts, src/stores/measurements/selectors.ts, src/stores/fit/selectors.ts, src/stores/degraded/selectors.ts</read_first>
  <action>Add only the selector and read-model additions needed by Phase 4. Implement `readyToAdvance` as a derived selector/read model only; do not store it. Lock it so it is true only when the required detection/runtime/catalog readiness inputs are satisfied and there is no blocking degraded issue. Create `src/lib/runtime/readModels.ts` as the cross-domain shopper-safe read surface for detection readiness, catalog readiness, Unity render readiness, measurement readiness, fit readiness, and degraded severity/blocking state. Keep these outputs free of raw event envelopes and raw transport fields.</action>
  <verify>rg -n "readyToAdvance|DetectionReadiness|CatalogReadiness|UnityRenderReadiness|MeasurementReadiness|FitReadiness|DegradedReadModel" src/stores/**/*.ts src/lib/runtime/readModels.ts</verify>
  <acceptance_criteria>
    - `readyToAdvance` is derived only and does not appear as stored state in any store.
    - `src/lib/runtime/readModels.ts` exposes the six locked shopper-safe runtime read-model families.
    - No selector or read-model file exposes raw transport payloads or bypasses existing store selectors.
  </acceptance_criteria>
  <done>The runtime layer exposes shopper-safe readiness and degraded state for Phase 5 screens without introducing new app modes or shopper phases.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize teardown/reset interaction points and add lifecycle/read-model tests</name>
  <files>src/app/runtime/createAppRuntime.ts, src/lib/runtime/degradedDerivation.test.ts, src/lib/runtime/readModels.test.ts, src/app/runtime/runtimeLifecycle.test.ts, src/lib/sessionReset/resetSession.test.ts</files>
  <read_first>src/app/runtime/createAppRuntime.ts, src/lib/sessionReset/resetSession.ts, src/lib/sessionReset/resetSession.test.ts, src/lib/runtime/degradedDerivation.ts, src/lib/runtime/readModels.ts</read_first>
  <action>Update `createAppRuntime.ts` so runtime teardown, restart, and reset interaction points are explicit and deterministic. The runtime must release subscriptions and ignore stale session-scoped events after reset without weakening local privacy wipe semantics. Add `degradedDerivation.test.ts` to verify taxonomy mapping and shopper-safe copy boundaries. Add `readModels.test.ts` to verify readiness derivation, including the locked `readyToAdvance` rule. Add `runtimeLifecycle.test.ts` to verify stop/restart subscription cleanup and reset interaction. Update `resetSession.test.ts` only as needed to prove runtime-driven late events cannot repopulate cleared shopper state.</action>
  <verify>npm run test -- --run src/lib/runtime/degradedDerivation.test.ts src/lib/runtime/readModels.test.ts src/app/runtime/runtimeLifecycle.test.ts src/lib/sessionReset/resetSession.test.ts</verify>
  <acceptance_criteria>
    - Lifecycle tests prove runtime stop/restart do not leak subscriptions or duplicate listeners.
    - Read-model tests prove `readyToAdvance` is false when any required readiness input is missing or a blocking degraded issue exists.
    - Reset tests prove late runtime events cannot repopulate cleared shopper state after reset.
  </acceptance_criteria>
  <done>The Phase 4 runtime lifecycle, degraded derivation, and readiness read models are locked down by focused automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/lib/runtime/degradedDerivation.test.ts src/lib/runtime/readModels.test.ts src/app/runtime/runtimeLifecycle.test.ts src/lib/sessionReset/resetSession.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "readyToAdvance|createAppRuntime|degraded|blocking|operational" src/lib/runtime/*.ts src/stores/**/*.ts src/app/runtime/*.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The runtime layer exposes calm degraded behavior, shopper-safe readiness models, and deterministic teardown/reset behavior without adding screens or redesigning state/contracts
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-seams-and-runtime-modes/04-03-SUMMARY.md`
</output>
