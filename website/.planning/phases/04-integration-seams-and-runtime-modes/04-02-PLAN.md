---
phase: 04-integration-seams-and-runtime-modes
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/adapters/runtime/createIntegrationRuntimePort.ts
  - src/adapters/runtime/runtimeTransport.ts
  - src/adapters/unity/createIntegrationUnityPort.ts
  - src/adapters/unity/unityTransport.ts
  - src/adapters/catalog/createIntegrationCatalogPort.ts
  - src/adapters/catalog/catalogSnapshotLoader.ts
  - src/lib/runtime/staleEventGuard.ts
  - src/lib/runtime/normalizeRuntimeEvent.ts
  - src/lib/runtime/normalizeUnityEvent.ts
  - src/lib/runtime/normalizeCatalogEvent.ts
  - src/lib/runtime/applyInboundEvent.ts
  - src/lib/runtime/normalization.test.ts
  - src/lib/runtime/staleEventGuard.test.ts
autonomous: true
requirements:
  - MODE-02
user_setup: []
must_haves:
  truths:
    - Integration-mode adapter implementations exist for RuntimePort, UnityPort, and CatalogPort without redesigning the port contracts.
    - Runtime normalization and mapping helpers sit between ports and store writes, with stale-event guards preventing late session events from repopulating cleared state.
    - Catalog loadSnapshot() is required, catalog subscribe() remains optional in v1, and no adapter writes stores directly.
  artifacts:
    - src/adapters/runtime/createIntegrationRuntimePort.ts
    - src/adapters/runtime/runtimeTransport.ts
    - src/adapters/unity/createIntegrationUnityPort.ts
    - src/adapters/unity/unityTransport.ts
    - src/adapters/catalog/createIntegrationCatalogPort.ts
    - src/adapters/catalog/catalogSnapshotLoader.ts
    - src/lib/runtime/staleEventGuard.ts
    - src/lib/runtime/normalizeRuntimeEvent.ts
    - src/lib/runtime/normalizeUnityEvent.ts
    - src/lib/runtime/normalizeCatalogEvent.ts
    - src/lib/runtime/applyInboundEvent.ts
    - src/lib/runtime/normalization.test.ts
    - src/lib/runtime/staleEventGuard.test.ts
  key_links:
    - src/lib/runtime/normalizeRuntimeEvent.ts, normalizeUnityEvent.ts, and normalizeCatalogEvent.ts isolate transport parsing from store-safe event handling.
    - src/lib/runtime/applyInboundEvent.ts is the only mapping layer allowed to translate normalized events into store writes.
    - src/lib/runtime/staleEventGuard.ts enforces the locked rule that late session-scoped events are ignored after reset or session replacement.
---

<objective>
Implement the integration-mode adapters and the normalization/mapping layer that safely connects them to the existing Phase 3 state surface.

Purpose: Make live runtime, Unity, and catalog inputs pluggable through isolated adapters and a single mapping layer without rewriting stores or presentation.
Output: Integration adapters, normalization/mapping helpers, stale-event guards, and focused tests for normalization and stale-session protection.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-01-PLAN.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@src/adapters/contracts/index.ts
@src/stores/session/sessionStore.ts
@src/stores/catalog/catalogStore.ts
@src/stores/measurements/measurementsStore.ts
@src/stores/fit/fitStore.ts
@src/stores/systemHealth/systemHealthStore.ts
@src/stores/degraded/degradedStore.ts
@src/lib/sessionReset/resetSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the integration-mode adapter modules for runtime, Unity, and local catalog</name>
  <files>src/adapters/runtime/createIntegrationRuntimePort.ts, src/adapters/runtime/runtimeTransport.ts, src/adapters/unity/createIntegrationUnityPort.ts, src/adapters/unity/unityTransport.ts, src/adapters/catalog/createIntegrationCatalogPort.ts, src/adapters/catalog/catalogSnapshotLoader.ts</files>
  <read_first>.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md, src/adapters/contracts/runtime.ts, src/adapters/contracts/unity.ts, src/adapters/contracts/catalog.ts</read_first>
  <action>Create the integration-mode adapter layer under `src/adapters/runtime/`, `src/adapters/unity/`, and `src/adapters/catalog/`. Keep transport wrappers isolated behind `runtimeTransport.ts`, `unityTransport.ts`, and `catalogSnapshotLoader.ts` so the port factories translate local transport payloads into the existing contract events. `CatalogPort.loadSnapshot()` is required and must be implemented. `CatalogPort.subscribe()` remains optional and should only be added if the local source supports low-friction change subscription in v1. Do not import stores, selectors, or presentation code into any adapter module.</action>
  <verify>rg -n "createIntegrationRuntimePort|createIntegrationUnityPort|createIntegrationCatalogPort|loadSnapshot\\(|subscribe\\?" src/adapters/runtime/*.ts src/adapters/unity/*.ts src/adapters/catalog/*.ts</verify>
  <acceptance_criteria>
    - Integration adapter factories exist for runtime, Unity, and catalog with no contract redesign.
    - `CatalogPort.loadSnapshot()` is implemented and `subscribe()` remains optional.
    - Adapter modules do not import from `@/stores`, `@/components`, or `@/app`.
  </acceptance_criteria>
  <done>The codebase has integration-mode adapter seams for all three ports while staying inside the existing contract layer.</done>
</task>

<task type="auto">
  <name>Task 2: Build the normalization, mapping, and stale-event guard layer</name>
  <files>src/lib/runtime/staleEventGuard.ts, src/lib/runtime/normalizeRuntimeEvent.ts, src/lib/runtime/normalizeUnityEvent.ts, src/lib/runtime/normalizeCatalogEvent.ts, src/lib/runtime/applyInboundEvent.ts</files>
  <read_first>.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md, src/stores/session/sessionStore.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/degraded/degradedStore.ts</read_first>
  <action>Create `staleEventGuard.ts` to enforce the locked rule that session-scoped events with stale or mismatched session IDs are ignored after reset or session replacement. Implement `normalizeRuntimeEvent.ts`, `normalizeUnityEvent.ts`, and `normalizeCatalogEvent.ts` so transport-specific payload handling is isolated from store writes. Implement `applyInboundEvent.ts` as the only layer that can translate normalized events into the existing store actions and reset-safe operational/degraded updates. Keep `readyToAdvance` out of stored state; phase advancement continues through existing session actions only.</action>
  <verify>rg -n "stale|sessionId|markDetectionReady|setSnapshot|setRecommendation|setSignals|setIssues|setGuidance" src/lib/runtime/*.ts</verify>
  <acceptance_criteria>
    - `applyInboundEvent.ts` is the only cross-store write layer between adapters and stores.
    - `staleEventGuard.ts` explicitly handles mismatched and late session-scoped events.
    - No normalizer or mapping helper imports presentation modules or creates new shopper phases.
  </acceptance_criteria>
  <done>The runtime pipeline has a single normalization/mapping layer with stale-event protection and explicit store-write ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for normalization and stale-session protection</name>
  <files>src/lib/runtime/normalization.test.ts, src/lib/runtime/staleEventGuard.test.ts</files>
  <read_first>src/lib/runtime/normalizeRuntimeEvent.ts, src/lib/runtime/normalizeUnityEvent.ts, src/lib/runtime/normalizeCatalogEvent.ts, src/lib/runtime/applyInboundEvent.ts, src/lib/runtime/staleEventGuard.ts</read_first>
  <action>Create `normalization.test.ts` to verify each inbound event family maps only to the allowed store-write surfaces and does not leak raw payload shapes into read-facing state. Create `staleEventGuard.test.ts` to verify that late session-scoped events do not repopulate cleared shopper state after `resetSession()` or runtime restart while app-scoped catalog and health updates can still apply. Keep these tests deterministic and local; do not require real network or Unity connections.</action>
  <verify>npm run test -- --run src/lib/runtime/normalization.test.ts src/lib/runtime/staleEventGuard.test.ts</verify>
  <acceptance_criteria>
    - `normalization.test.ts` covers runtime, Unity, and catalog event families.
    - `staleEventGuard.test.ts` covers stale-event ignore behavior after both reset and session replacement.
    - `npm run test -- --run src/lib/runtime/normalization.test.ts src/lib/runtime/staleEventGuard.test.ts` exits 0.
  </acceptance_criteria>
  <done>The highest-risk event-mapping and stale-session behaviors are covered before degraded/read-model work builds on them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/lib/runtime/normalization.test.ts src/lib/runtime/staleEventGuard.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "createIntegrationRuntimePort|createIntegrationUnityPort|createIntegrationCatalogPort|applyInboundEvent|staleEventGuard" src/adapters/**/*.ts src/lib/runtime/*.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Integration-mode adapters and the normalization/mapping pipeline exist without leaking transport logic into stores or presentation
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-seams-and-runtime-modes/04-02-SUMMARY.md`
</output>
