---
phase: 04-integration-seams-and-runtime-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/runtime/runtimeSourceMode.ts
  - src/app/runtime/runtimeConfig.ts
  - src/app/runtime/createRuntimeDependencies.ts
  - src/app/runtime/createAppRuntime.ts
  - src/app/App.tsx
  - src/adapters/runtime/createDemoRuntimePort.ts
  - src/adapters/unity/createDemoUnityPort.ts
  - src/adapters/catalog/createDemoCatalogPort.ts
  - src/mocks/catalog/catalogFixtures.ts
  - src/mocks/runtime/runtimeFixtures.ts
  - src/mocks/runtime/demoScenarioDriver.ts
  - src/mocks/unity/unityFixtures.ts
  - src/app/runtime/createAppRuntime.test.ts
  - src/adapters/ports.test.ts
autonomous: true
requirements:
  - MODE-01
  - MODE-02
user_setup: []
must_haves:
  truths:
    - The app exposes one runtime bootstrap/config seam that resolves `demo | integration` source mode before any adapter startup.
    - Demo-mode `RuntimePort`, `UnityPort`, and `CatalogPort` implementations exist without changing Phase 3 contracts or leaking transport logic into presentation.
    - Demo fixtures and a scenario driver can exercise the full shopper path through the existing stores and orchestrator boundary before live backend maturity exists.
  artifacts:
    - src/app/runtime/runtimeSourceMode.ts
    - src/app/runtime/runtimeConfig.ts
    - src/app/runtime/createRuntimeDependencies.ts
    - src/app/runtime/createAppRuntime.ts
    - src/adapters/runtime/createDemoRuntimePort.ts
    - src/adapters/unity/createDemoUnityPort.ts
    - src/adapters/catalog/createDemoCatalogPort.ts
    - src/mocks/catalog/catalogFixtures.ts
    - src/mocks/runtime/runtimeFixtures.ts
    - src/mocks/runtime/demoScenarioDriver.ts
    - src/mocks/unity/unityFixtures.ts
    - src/app/runtime/createAppRuntime.test.ts
    - src/adapters/ports.test.ts
  key_links:
    - src/app/runtime/runtimeConfig.ts locks bootstrap/config-driven mode selection and keeps manual switching dev-only or admin-only.
    - src/app/runtime/createAppRuntime.ts is the single composition root for startup, subscriptions, teardown, restart, and dependency ownership.
    - src/adapters/catalog/createDemoCatalogPort.ts must implement required loadSnapshot() and may omit subscribe() in Phase 4 v1.
---

<objective>
Create the runtime bootstrap/config seam, demo-mode adapter implementations, and deterministic fixtures needed to make the app runnable end-to-end in demo mode.

Purpose: Establish the app runtime composition root and the demo path first, without changing the shell, shopper screens, or Phase 3 contracts.
Output: Runtime source-mode/config modules, demo port implementations, demo fixtures/simulator, minimal app bootstrap wiring, and focused runtime bootstrap tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/03-state-model-and-typed-contracts/03-01-PLAN.md
@.planning/phases/03-state-model-and-typed-contracts/03-02-PLAN.md
@.planning/phases/03-state-model-and-typed-contracts/03-03-PLAN.md
@src/app/App.tsx
@src/stores/session/sessionStore.ts
@src/stores/catalog/catalogStore.ts
@src/stores/measurements/measurementsStore.ts
@src/stores/fit/fitStore.ts
@src/stores/systemHealth/systemHealthStore.ts
@src/stores/degraded/degradedStore.ts
@src/lib/sessionReset/resetSession.ts
@src/adapters/contracts/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the runtime bootstrap/config and dependency composition modules</name>
  <files>src/app/runtime/runtimeSourceMode.ts, src/app/runtime/runtimeConfig.ts, src/app/runtime/createRuntimeDependencies.ts, src/app/runtime/createAppRuntime.ts, src/app/App.tsx</files>
  <read_first>.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md, src/app/App.tsx, src/adapters/contracts/ports.ts, src/lib/sessionReset/resetSession.ts</read_first>
  <action>Create the new `src/app/runtime/` boundary. Implement `runtimeSourceMode.ts` with `demo | integration` only. Implement `runtimeConfig.ts` so source-mode selection is bootstrap/config driven and manual switching is disabled by default, with any override path explicitly limited to dev/admin usage. Implement `createRuntimeDependencies.ts` to resolve the three port implementations for the selected mode. Implement `createAppRuntime.ts` as the single runtime composition root exposing `start()`, `stop()`, `restart()`, and `getSourceMode()` and owning adapter startup, subscription installation, teardown, and dependency lifecycle only. Update `src/app/App.tsx` only as needed to bootstrap the runtime without changing the shell composition or making components transport-aware.</action>
  <verify>rg -n "type RuntimeSourceMode = 'demo' \\| 'integration'|createAppRuntime|start\\(\\): Promise<void>|stop\\(\\): Promise<void>|restart\\(" src/app/runtime/*.ts src/app/App.tsx</verify>
  <acceptance_criteria>
    - `src/app/runtime/runtimeSourceMode.ts` contains only `demo` and `integration`.
    - `src/app/runtime/runtimeConfig.ts` defaults manual switching off and does not create a shopper-facing mode toggle.
    - `src/app/runtime/createAppRuntime.ts` owns runtime lifecycle and does not import from `@/components` or shopper/admin screen modules.
  </acceptance_criteria>
  <done>The app has a single bootstrap/config-driven runtime seam ready to host both demo and integration adapters.</done>
</task>

<task type="auto">
  <name>Task 2: Implement demo-mode RuntimePort, UnityPort, and CatalogPort plus deterministic fixtures</name>
  <files>src/adapters/runtime/createDemoRuntimePort.ts, src/adapters/unity/createDemoUnityPort.ts, src/adapters/catalog/createDemoCatalogPort.ts, src/mocks/catalog/catalogFixtures.ts, src/mocks/runtime/runtimeFixtures.ts, src/mocks/runtime/demoScenarioDriver.ts, src/mocks/unity/unityFixtures.ts</files>
  <read_first>.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md, src/adapters/contracts/runtime.ts, src/adapters/contracts/unity.ts, src/adapters/contracts/catalog.ts, src/types/catalog.ts, src/types/measurements.ts, src/types/fit.ts, src/types/system.ts</read_first>
  <action>Create the demo port implementations under `src/adapters/`, not under presentation code. Implement `createDemoCatalogPort.ts` with required `loadSnapshot()` and optional omission of `subscribe()` for v1. Implement `createDemoRuntimePort.ts` and `createDemoUnityPort.ts` to emit the existing contract events only, driven by deterministic local fixtures and a reusable `demoScenarioDriver.ts`. Create catalog, runtime, and Unity fixture modules with enough data to exercise happy-path, partial-data, delayed-render, unavailable, and recovery scenarios. Keep fixtures honest to the upper-body kiosk prototype and avoid shopper screens, admin workflows, or cloud/backend-specific payload shapes.</action>
  <verify>rg -n "loadSnapshot\\(|RuntimeInboundEvent|UnityInboundEvent|catalog.snapshot.updated|runtime.scan.completed|unity.render.stateUpdated" src/adapters/runtime/*.ts src/adapters/unity/*.ts src/adapters/catalog/*.ts src/mocks/**/*.ts</verify>
  <acceptance_criteria>
    - `src/adapters/catalog/createDemoCatalogPort.ts` implements `loadSnapshot()` and does not require `subscribe()`.
    - Demo adapters emit only normalized Phase 3 contract events and do not write stores directly.
    - Fixture modules include catalog, measurement, fit, health, and Unity render scenarios needed for the full shopper path plus degraded cases.
  </acceptance_criteria>
  <done>Demo mode can drive the Phase 3 state layer through real port implementations and deterministic local fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for bootstrap mode selection and demo adapter behavior</name>
  <files>src/app/runtime/createAppRuntime.test.ts, src/adapters/ports.test.ts</files>
  <read_first>src/app/runtime/createAppRuntime.ts, src/app/runtime/runtimeConfig.ts, src/adapters/runtime/createDemoRuntimePort.ts, src/adapters/unity/createDemoUnityPort.ts, src/adapters/catalog/createDemoCatalogPort.ts</read_first>
  <action>Create `src/app/runtime/createAppRuntime.test.ts` to verify config-driven source-mode resolution, disabled-by-default manual switching, and that `start()/stop()/restart()` do not duplicate subscriptions. Create `src/adapters/ports.test.ts` to verify the demo ports conform to the existing port contracts, `CatalogPort.loadSnapshot()` is required and returns normalized data, and demo scenario emission follows the contract event families without bypassing the orchestrator boundary. Keep the tests transport-free and deterministic.</action>
  <verify>npm run test -- --run src/app/runtime/createAppRuntime.test.ts src/adapters/ports.test.ts</verify>
  <acceptance_criteria>
    - `src/app/runtime/createAppRuntime.test.ts` asserts `demo | integration` mode selection and lifecycle idempotence.
    - `src/adapters/ports.test.ts` asserts required demo catalog snapshot loading and normalized event emission for runtime and Unity.
    - `npm run test -- --run src/app/runtime/createAppRuntime.test.ts src/adapters/ports.test.ts` exits 0.
  </acceptance_criteria>
  <done>The runtime bootstrap seam and demo port implementations are covered by focused automated tests before integration adapters are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/runtime/createAppRuntime.test.ts src/adapters/ports.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "type RuntimeSourceMode = 'demo' \\| 'integration'|createAppRuntime|loadSnapshot\\(" src/app/runtime/*.ts src/adapters/**/*.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a bootstrap-configured runtime composition root and a deterministic demo path without touching shopper screens or redesigning state/contracts
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-seams-and-runtime-modes/04-01-SUMMARY.md`
</output>
