---
phase: 06-admin-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/shell/AdminShell.tsx
  - src/app/shell/AppShell.test.tsx
  - src/app/App.test.tsx
  - src/screens/shopper/IdleScreen.tsx
  - src/features/admin/adminSectionLayout.ts
  - src/features/admin/AdminSectionRenderer.tsx
  - src/features/admin/navigation/AdminSectionNav.tsx
  - src/features/admin/access/AdminPinEntry.tsx
  - src/features/admin/dashboard/AdminSummaryCard.tsx
  - src/features/admin/dashboard/DashboardQuickLinks.tsx
  - src/features/admin/dashboard/HealthSummaryPanel.tsx
  - src/features/admin/dashboard/CalibrationSummaryPanel.tsx
  - src/features/admin/dashboard/CatalogSummaryPanel.tsx
  - src/features/admin/dashboard/LogSummaryPanel.tsx
  - src/features/admin/readModels/access.ts
  - src/features/admin/readModels/dashboard.ts
  - src/features/admin/readModels/index.ts
  - src/screens/admin/AccessScreen.tsx
  - src/screens/admin/DashboardScreen.tsx
  - src/screens/admin/index.ts
  - src/screens/admin/AccessScreen.test.tsx
  - src/screens/admin/DashboardScreen.test.tsx
autonomous: true
requirements:
  - ADMIN-01
user_setup: []
must_haves:
  truths:
    - The existing Phase 2 admin shell remains the only admin shell frame, but its command bar, nav rail, workspace, and inspector are now driven by a real admin section-layout renderer rather than proof content.
    - Admin entry is a dedicated six-digit local PIN-gated access surface and the public shell-mode toggle is no longer the production access path.
    - The dashboard is a real landing workspace with health, calibration, catalog, and log summaries plus quick links into the fixed admin sections.
  artifacts:
    - src/features/admin/adminSectionLayout.ts
    - src/features/admin/AdminSectionRenderer.tsx
    - src/features/admin/navigation/AdminSectionNav.tsx
    - src/features/admin/access/AdminPinEntry.tsx
    - src/features/admin/readModels/access.ts
    - src/features/admin/readModels/dashboard.ts
    - src/screens/admin/AccessScreen.tsx
    - src/screens/admin/DashboardScreen.tsx
    - src/screens/admin/AccessScreen.test.tsx
    - src/screens/admin/DashboardScreen.test.tsx
  key_links:
    - `src/features/admin/adminSectionLayout.ts` becomes the slot contract that maps each admin surface into the existing shell zones without redesigning `AdminShell`.
    - `src/features/admin/AdminSectionRenderer.tsx` is the only admin surface-selection seam; `AdminShell` remains a shell composer rather than a workflow switchboard.
    - `AccessScreen.tsx` and `DashboardScreen.tsx` must consume selectors/read models only and must not import adapters, runtime ports, or transport payload types.
---

<objective>
Implement the admin shell host boundary, dedicated staff-access flow, and real dashboard landing workspace.

Purpose: Replace the proof-only admin shell with a production-minded shell host, PIN-gated access surface, fixed left-nav routing, and a real operational dashboard without redesigning shell composition, shopper flow, or runtime architecture.
Output: Admin section-layout contract, admin renderer, dedicated access screen, dashboard screen, summary modules, and focused shell/access/dashboard tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-surface/06-CONTEXT.md
@.planning/phases/06-admin-surface/06-UI-SPEC.md
@.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/04-integration-seams-and-runtime-modes/04-CONTRACT-SPEC.md
@src/app/shell/AdminShell.tsx
@src/app/shell/AppShell.tsx
@src/app/shell/AppShell.test.tsx
@src/app/App.test.tsx
@src/screens/shopper/IdleScreen.tsx
@src/stores/admin/adminStore.ts
@src/stores/admin/selectors.ts
@src/stores/systemHealth/selectors.ts
@src/stores/catalog/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the admin section-layout and rendering seam without changing shell geometry</name>
  <files>src/features/admin/adminSectionLayout.ts, src/features/admin/AdminSectionRenderer.tsx, src/features/admin/navigation/AdminSectionNav.tsx, src/app/shell/AdminShell.tsx, src/screens/admin/index.ts</files>
  <read_first>.planning/phases/06-admin-surface/06-UI-SPEC.md, src/app/shell/AdminShell.tsx, src/stores/admin/selectors.ts</read_first>
  <action>Create `src/features/admin/adminSectionLayout.ts` exporting a concrete `AdminSectionLayout` type with the exact fields `commandBar`, `nav`, `workspace`, and `inspector`, with `commandBar`, `nav`, and `workspace` required and `inspector` nullable. Create `src/features/admin/AdminSectionRenderer.tsx` as the only admin surface-selection seam. It must read `selectAdminState()` plus `readAdminAccessGate()` and route `requested` access to `createAdminAccessScreenLayout()`, `granted + dashboard` to `createAdminDashboardScreenLayout()`, and leave extension points for `catalog`, `calibration`, and `logs` without implementing them yet. Create `src/features/admin/navigation/AdminSectionNav.tsx` for the fixed four-section nav list `Dashboard`, `Catalog`, `Calibration`, and `Logs`. Update `src/app/shell/AdminShell.tsx` so it preserves `ShellFrame`, `ShellBand`, `AdminNavRail`, `WorkspaceCanvas`, and `InspectorRail`, but renders the returned layout nodes instead of `adminProofContent` and `ShellModeToggle`. Create `src/screens/admin/index.ts` exporting the admin screen layout creators.</action>
  <verify>rg -n "export type AdminSectionLayout|commandBar: ReactNode|workspace: ReactNode|inspector" src/features/admin/adminSectionLayout.ts && rg -n "readAdminAccessGate|createAdminAccessScreenLayout|createAdminDashboardScreenLayout|Dashboard|Catalog|Calibration|Logs" src/features/admin/AdminSectionRenderer.tsx src/features/admin/navigation/AdminSectionNav.tsx && ! rg -n "ShellModeToggle|adminProofContent" src/app/shell/AdminShell.tsx</verify>
  <acceptance_criteria>
    - `src/features/admin/adminSectionLayout.ts` contains `export type AdminSectionLayout`.
    - `src/features/admin/AdminSectionRenderer.tsx` contains `readAdminAccessGate`, `createAdminAccessScreenLayout`, and `createAdminDashboardScreenLayout`.
    - `src/features/admin/navigation/AdminSectionNav.tsx` renders the four fixed section labels from the UI-SPEC.
    - `src/app/shell/AdminShell.tsx` still contains `ShellBand`, `AdminNavRail`, `WorkspaceCanvas`, and `InspectorRail`, but no longer imports `ShellModeToggle` or `proofContent`.
  </acceptance_criteria>
  <done>The admin shell is ready to host real admin surfaces through a stable slot contract without redesigning the Phase 2 shell frame.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the dedicated six-digit staff access surface and real dashboard landing workspace</name>
  <files>src/screens/shopper/IdleScreen.tsx, src/features/admin/access/AdminPinEntry.tsx, src/features/admin/dashboard/AdminSummaryCard.tsx, src/features/admin/dashboard/DashboardQuickLinks.tsx, src/features/admin/dashboard/HealthSummaryPanel.tsx, src/features/admin/dashboard/CalibrationSummaryPanel.tsx, src/features/admin/dashboard/CatalogSummaryPanel.tsx, src/features/admin/dashboard/LogSummaryPanel.tsx, src/features/admin/readModels/access.ts, src/features/admin/readModels/dashboard.ts, src/features/admin/readModels/index.ts, src/screens/admin/AccessScreen.tsx, src/screens/admin/DashboardScreen.tsx, src/screens/admin/index.ts</files>
  <read_first>.planning/phases/06-admin-surface/06-UI-SPEC.md, src/screens/shopper/IdleScreen.tsx, src/stores/session/selectors.ts, src/stores/admin/selectors.ts, src/stores/systemHealth/selectors.ts, src/stores/catalog/selectors.ts</read_first>
  <action>Create `src/features/admin/readModels/access.ts` exporting `readAdminAccessGate()` that derives whether staff entry is blocked by an active shopper session, available from idle, requested, or granted using `selectCanStartSession()`, `selectCanEndSession()`, `selectAdminState()`, and `selectUiMode()`. Create `src/features/admin/readModels/dashboard.ts` exporting the dashboard summary read models locked by the UI-SPEC. Implement `src/features/admin/access/AdminPinEntry.tsx` with six masked PIN cells, numeric keypad support, `Unlock Admin`, and `Cancel`, driven by local UI state only. Implement the four dashboard summary modules plus `AdminSummaryCard.tsx` and `DashboardQuickLinks.tsx` so the dashboard shows health, calibration, catalog, and log summaries plus quick links into the fixed sections. Implement `src/screens/admin/AccessScreen.tsx` exporting `createAdminAccessScreenLayout()` and `src/screens/admin/DashboardScreen.tsx` exporting `createAdminDashboardScreenLayout()`. Update `src/screens/shopper/IdleScreen.tsx` to expose only a subdued `Staff Access` utility entry point and never promote it over `Start Session`.</action>
  <verify>rg -n "readAdminAccessGate|selectCanStartSession|selectCanEndSession|selectAdminState|selectUiMode" src/features/admin/readModels/access.ts && rg -n "selectCalibrationState|selectVisibleLogs|selectHealthSignals|selectWorstSystemStatus|selectCatalogStatus|selectVisibleGarments|selectVisibleCategories" src/features/admin/readModels/dashboard.ts && rg -n "Unlock Admin|Cancel|Backspace|Enter" src/features/admin/access/AdminPinEntry.tsx && rg -n "Staff Access|Start Session" src/screens/shopper/IdleScreen.tsx</verify>
  <acceptance_criteria>
    - `src/features/admin/readModels/access.ts` contains every selector named in the action text and does not import adapters or transport helpers.
    - `src/features/admin/access/AdminPinEntry.tsx` contains six-digit PIN semantics, `Unlock Admin`, and `Cancel`.
    - `src/screens/admin/DashboardScreen.tsx` renders the four summary surfaces and quick links, not placeholder shell copy.
    - `src/screens/shopper/IdleScreen.tsx` contains `Staff Access`, but `Start Session` remains the primary CTA.
  </acceptance_criteria>
  <done>The app has a dedicated staff-access surface and a real dashboard landing workspace without altering shopper flow structure or shell geometry.</done>
</task>

<task type="auto">
  <name>Task 3: Add shell, access, and dashboard interaction tests and remove proof-only assumptions</name>
  <files>src/app/shell/AppShell.test.tsx, src/app/App.test.tsx, src/screens/admin/AccessScreen.test.tsx, src/screens/admin/DashboardScreen.test.tsx</files>
  <read_first>src/app/shell/AppShell.test.tsx, src/app/App.test.tsx, src/screens/admin/AccessScreen.tsx, src/screens/admin/DashboardScreen.tsx</read_first>
  <action>Update `src/app/shell/AppShell.test.tsx` and `src/app/App.test.tsx` so admin-mode assertions target the real access/dashboard shell landmarks and no longer assume `ShellModeToggle` or placeholder proof copy. Create `src/screens/admin/AccessScreen.test.tsx` covering the six-digit PIN flow, disabled-until-complete unlock behavior, invalid-PIN reset behavior, and absence of shopper-specific data. Create `src/screens/admin/DashboardScreen.test.tsx` covering the four summary panels, quick links, and nav selection state. Keep tests selector- and read-model-driven; do not import runtime adapters or contract event modules.</action>
  <verify>npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/screens/admin/AccessScreen.test.tsx src/screens/admin/DashboardScreen.test.tsx</verify>
  <acceptance_criteria>
    - `src/app/App.test.tsx` no longer asserts `Admin Mode` button behavior from the proof-only shell toggle.
    - `src/screens/admin/AccessScreen.test.tsx` contains assertions for disabled-until-complete unlock, invalid PIN handling, and absence of shopper measurements/selection copy.
    - `src/screens/admin/DashboardScreen.test.tsx` contains assertions for `Health Summary`, `Calibration Summary`, `Catalog Summary`, and `Log Summary`.
    - `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/screens/admin/AccessScreen.test.tsx src/screens/admin/DashboardScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The real admin entry and dashboard surfaces are covered by focused interaction tests and the proof-shell assumptions are removed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/screens/admin/AccessScreen.test.tsx src/screens/admin/DashboardScreen.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "export type AdminSectionLayout|readAdminAccessGate|Unlock Admin|Health Summary|Calibration Summary|Catalog Summary|Log Summary" src/features/admin/**/*.ts* src/screens/admin/*.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The admin shell now hosts a dedicated staff-access surface and a real dashboard landing workspace through a stable layout-rendering seam without redesigning earlier phases
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-surface/06-01-SUMMARY.md`
</output>
