---
phase: 06-admin-surface
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/types/adminCatalog.ts
  - src/stores/admin/adminStore.ts
  - src/stores/admin/selectors.ts
  - src/features/admin/readModels/catalog.ts
  - src/features/admin/readModels/index.ts
  - src/features/admin/catalog/CatalogSummaryStrip.tsx
  - src/features/admin/catalog/CatalogFilterBar.tsx
  - src/features/admin/catalog/CatalogGarmentTable.tsx
  - src/features/admin/catalog/CatalogInspectorEditor.tsx
  - src/features/admin/AdminSectionRenderer.tsx
  - src/screens/admin/CatalogScreen.tsx
  - src/screens/admin/index.ts
  - src/app/runtime/createAppRuntime.ts
  - src/app/runtime/runtimeLifecycle.test.ts
  - src/screens/admin/CatalogScreen.test.tsx
  - src/screens/admin/AdminFlow.test.tsx
autonomous: true
requirements:
  - ADMIN-03
user_setup: []
must_haves:
  truths:
    - Catalog curation is implemented as a small admin metadata overlay, not by distorting shopper-facing catalog entities or introducing a CMS workflow.
    - Catalog curation and refresh remain local operational controls only and stay inside the existing store/orchestrator boundaries.
    - The phase ends with admin interaction coverage proving access, section navigation, catalog curation, and return-to-shopper behavior without concurrent shopper/admin mode.
  artifacts:
    - src/types/adminCatalog.ts
    - src/stores/admin/adminStore.ts
    - src/stores/admin/selectors.ts
    - src/features/admin/readModels/catalog.ts
    - src/features/admin/catalog/CatalogSummaryStrip.tsx
    - src/features/admin/catalog/CatalogFilterBar.tsx
    - src/features/admin/catalog/CatalogGarmentTable.tsx
    - src/features/admin/catalog/CatalogInspectorEditor.tsx
    - src/screens/admin/CatalogScreen.tsx
    - src/screens/admin/CatalogScreen.test.tsx
    - src/screens/admin/AdminFlow.test.tsx
  key_links:
    - `src/types/adminCatalog.ts` is the separate metadata shape for enabled/disabled state, category assignment, ordering, default variant, default size, and default color.
    - `src/stores/admin/adminStore.ts` owns committed curation metadata and catalog-refresh intent; shopper-facing catalog entities remain in `src/stores/catalog/`.
    - `src/app/runtime/createAppRuntime.ts` may only add the minimal admin refresh hook needed to re-run existing catalog snapshot loading; no raw transport access is allowed in JSX and no broader contract redesign is planned.
---

<objective>
Implement the local catalog curation workflow, minimal admin metadata/state extensions, and final admin interaction verification.

Purpose: Finish Phase 6 with a real local catalog manager, staged inspector editing, refresh orchestration through existing seams, and phase-level admin flow coverage without expanding into product authoring or cloud tooling.
Output: Separate admin catalog metadata types, admin-store curation overlay, catalog read models and modules, refresh orchestration, catalog tests, and final admin flow tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-surface/06-CONTEXT.md
@.planning/phases/06-admin-surface/06-UI-SPEC.md
@.planning/phases/06-admin-surface/06-01-PLAN.md
@.planning/phases/06-admin-surface/06-02-PLAN.md
@src/stores/admin/adminStore.ts
@src/stores/admin/selectors.ts
@src/stores/catalog/catalogStore.ts
@src/stores/catalog/selectors.ts
@src/app/runtime/createAppRuntime.ts
@src/mocks/catalog/catalogFixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the separate admin catalog metadata shape and minimal admin-store curation overlay</name>
  <files>src/types/adminCatalog.ts, src/stores/admin/adminStore.ts, src/stores/admin/selectors.ts, src/features/admin/readModels/catalog.ts, src/features/admin/readModels/index.ts</files>
  <read_first>.planning/phases/06-admin-surface/06-UI-SPEC.md, src/stores/admin/adminStore.ts, src/stores/admin/selectors.ts, src/stores/catalog/selectors.ts, src/types/admin.ts</read_first>
  <action>Create `src/types/adminCatalog.ts` with the small committed curation metadata shape for `enabled`, `categoryId`, `sortOrder`, `defaultVariantId`, `defaultSizeCode`, and `defaultColorId`. Extend `src/stores/admin/adminStore.ts` with the minimal admin-only state needed to hold committed curation metadata and a catalog refresh intent token; do not move shopper catalog entities into the admin store. Extend `src/stores/admin/selectors.ts` with admin-catalog accessors that remain metadata-only. Create `src/features/admin/readModels/catalog.ts` exporting `readAdminCatalogWorkspace()`, `readAdminCatalogInspector()`, and `readCatalogCurationSummary()` by composing admin metadata plus existing catalog selectors. Update `src/features/admin/readModels/index.ts` accordingly.</action>
  <verify>rg -n "defaultVariantId|defaultSizeCode|defaultColorId|sortOrder|enabled" src/types/adminCatalog.ts src/stores/admin/adminStore.ts src/stores/admin/selectors.ts && rg -n "readAdminCatalogWorkspace|readAdminCatalogInspector|readCatalogCurationSummary" src/features/admin/readModels/catalog.ts</verify>
  <acceptance_criteria>
    - `src/types/adminCatalog.ts` contains the six metadata fields named in the action text.
    - `src/stores/admin/adminStore.ts` owns committed curation metadata and a refresh intent only; it does not copy full garment entities into admin state.
    - `src/features/admin/readModels/catalog.ts` exports the three exact read-model names from the action text.
    - No file in `<files>` imports adapter transports or rewrites shopper-facing catalog entity types.
  </acceptance_criteria>
  <done>The admin catalog manager has a dedicated metadata boundary that stays separate from shopper-facing catalog entities and existing store ownership rules.</done>
</task>

<task type="auto">
  <name>Task 2: Build the catalog curation workspace, inspector editing flow, and section routing</name>
  <files>src/features/admin/catalog/CatalogSummaryStrip.tsx, src/features/admin/catalog/CatalogFilterBar.tsx, src/features/admin/catalog/CatalogGarmentTable.tsx, src/features/admin/catalog/CatalogInspectorEditor.tsx, src/features/admin/AdminSectionRenderer.tsx, src/screens/admin/CatalogScreen.tsx, src/screens/admin/index.ts</files>
  <read_first>.planning/phases/06-admin-surface/06-UI-SPEC.md, src/features/admin/readModels/catalog.ts, src/components/primitives/index.ts</read_first>
  <action>Create the catalog summary strip, filter bar, garment table, and inspector editor modules exactly around the locked Phase 6 curation scope. Implement `src/screens/admin/CatalogScreen.tsx` exporting `createAdminCatalogScreenLayout()` that renders the summary strip plus filters in the workspace header, the garment table in the workspace body, and the selected-garment curation editor in the inspector. Use screen-local UI state for row selection, filters, and staged unsaved draft values; keep committed metadata in the admin store only. Update `src/features/admin/AdminSectionRenderer.tsx` and `src/screens/admin/index.ts` so `catalog` resolves to the new screen layout and stays inside the existing admin shell.</action>
  <verify>rg -n "createAdminCatalogScreenLayout|Save Curation|Discard Changes|Refresh Snapshot|Move Up|Move Down" src/screens/admin/CatalogScreen.tsx src/features/admin/catalog/*.tsx && rg -n "catalog" src/features/admin/AdminSectionRenderer.tsx</verify>
  <acceptance_criteria>
    - `src/screens/admin/CatalogScreen.tsx` contains `Save Curation`, `Discard Changes`, and `Refresh Snapshot`.
    - `src/features/admin/catalog/CatalogInspectorEditor.tsx` contains controls for enable/disable, category, ordering, default variant, default size, and default color.
    - Screen-local UI state is used for selected row, filters, and unsaved drafts; committed curation metadata stays in the admin store.
    - No file in `<files>` adds product-authoring, upload, pricing, or cloud-inventory affordances.
  </acceptance_criteria>
  <done>The admin surface now contains a real local catalog manager with inspector-based staged editing and no CMS-style scope creep.</done>
</task>

<task type="auto">
  <name>Task 3: Wire refresh through existing catalog-loading seams and add final catalog/admin-flow verification</name>
  <files>src/app/runtime/createAppRuntime.ts, src/app/runtime/runtimeLifecycle.test.ts, src/screens/admin/CatalogScreen.test.tsx, src/screens/admin/AdminFlow.test.tsx</files>
  <read_first>src/app/runtime/createAppRuntime.ts, src/stores/admin/adminStore.ts, src/screens/admin/CatalogScreen.tsx, src/screens/admin/AccessScreen.tsx, src/screens/admin/DashboardScreen.tsx</read_first>
  <action>Extend `src/app/runtime/createAppRuntime.ts` only enough to observe the admin catalog refresh intent and re-run the existing catalog snapshot load path through `CatalogPort.loadSnapshot()`; do not introduce raw runtime access in JSX and do not add a new transport architecture. Update `src/app/runtime/runtimeLifecycle.test.ts` to verify admin-triggered refresh reuses the existing catalog loading path without breaking shopper reset guarantees. Create `src/screens/admin/CatalogScreen.test.tsx` covering row selection, staged edits, save/discard behavior, and refresh-discard confirmation when unsaved changes exist. Create `src/screens/admin/AdminFlow.test.tsx` as the phase-level suite covering idle `Staff Access` -> valid PIN -> dashboard -> section navigation -> catalog curation save -> return to shopper, with an assertion that admin entry is unavailable during an active shopper session.</action>
  <verify>npm run test -- --run src/app/runtime/runtimeLifecycle.test.ts src/screens/admin/CatalogScreen.test.tsx src/screens/admin/AdminFlow.test.tsx</verify>
  <acceptance_criteria>
    - `src/app/runtime/createAppRuntime.ts` reuses the existing catalog snapshot load path for admin refresh and does not import admin screen modules.
    - `src/screens/admin/CatalogScreen.test.tsx` contains assertions for staged edit, save/discard, and refresh-with-unsaved-changes behavior.
    - `src/screens/admin/AdminFlow.test.tsx` contains assertions for blocked access during active shopper session, valid PIN access, section navigation, catalog curation save, and return to shopper mode.
    - No files under `src/adapters/contracts/` are modified unless execution discovers an unavoidable blocker; default Phase 6 implementation should complete without contract changes.
  </acceptance_criteria>
  <done>The catalog manager and the full admin access/navigation flow are verified end to end while staying within the existing store and runtime seam architecture.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/runtime/runtimeLifecycle.test.ts src/screens/admin/CatalogScreen.test.tsx src/screens/admin/AdminFlow.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "createAdminCatalogScreenLayout|readAdminCatalogWorkspace|Staff Access|Unlock Admin|Return to Shopper|Save Curation" src/screens/admin/*.tsx src/features/admin/**/*.ts*`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 ends with a real local catalog manager, refresh orchestration through existing seams, and phase-level admin interaction coverage without redesigning earlier architecture
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-surface/06-03-SUMMARY.md`
</output>
