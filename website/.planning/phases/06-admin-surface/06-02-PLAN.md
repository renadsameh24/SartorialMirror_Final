---
phase: 06-admin-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/stores/admin/selectors.ts
  - src/mocks/admin/adminFixtures.ts
  - src/features/admin/readModels/calibration.ts
  - src/features/admin/readModels/logs.ts
  - src/features/admin/readModels/index.ts
  - src/features/admin/calibration/CalibrationStatusPanel.tsx
  - src/features/admin/calibration/CalibrationChecklist.tsx
  - src/features/admin/calibration/CalibrationActionPanel.tsx
  - src/features/admin/logs/LogFilterBar.tsx
  - src/features/admin/logs/LogTable.tsx
  - src/features/admin/logs/LogInspectorDetail.tsx
  - src/features/admin/AdminSectionRenderer.tsx
  - src/screens/admin/CalibrationScreen.tsx
  - src/screens/admin/LogsScreen.tsx
  - src/screens/admin/index.ts
  - src/screens/admin/CalibrationScreen.test.tsx
  - src/screens/admin/LogsScreen.test.tsx
autonomous: true
requirements:
  - ADMIN-02
user_setup: []
must_haves:
  truths:
    - Calibration and logs render as real admin workspaces inside the existing admin shell, not as placeholder panels or new page frames.
    - Calibration, log summary, and log detail consume only admin selectors and admin read models built from normalized store state.
    - Admin health visibility remains normalized and staff-readable; no raw payload dumps, JSON views, or transport jargon leak into the admin surfaces.
  artifacts:
    - src/mocks/admin/adminFixtures.ts
    - src/features/admin/readModels/calibration.ts
    - src/features/admin/readModels/logs.ts
    - src/features/admin/calibration/CalibrationStatusPanel.tsx
    - src/features/admin/calibration/CalibrationChecklist.tsx
    - src/features/admin/calibration/CalibrationActionPanel.tsx
    - src/features/admin/logs/LogFilterBar.tsx
    - src/features/admin/logs/LogTable.tsx
    - src/features/admin/logs/LogInspectorDetail.tsx
    - src/screens/admin/CalibrationScreen.tsx
    - src/screens/admin/LogsScreen.tsx
    - src/screens/admin/CalibrationScreen.test.tsx
    - src/screens/admin/LogsScreen.test.tsx
  key_links:
    - `CalibrationScreen.tsx` must consume `selectAdminState()`, `selectCalibrationState()`, `selectHealthSignals()`, `selectOperationalStatuses()`, and `selectOperationalStatus(surface)` through admin read models rather than inline store joining.
    - `LogsScreen.tsx` must consume `selectAdminState()`, `selectVisibleLogs()`, and normalized log read models only.
    - `src/mocks/admin/adminFixtures.ts` is the local seeding surface for operational logs and calibration state in tests and demo-safe UI verification.
---

<objective>
Implement the calibration and logs admin workspaces plus the read-model layer and fixtures they depend on.

Purpose: Deliver real operational workspaces for calibration controls, health/status visibility, and local log/error inspection without changing shell composition or leaking runtime details into JSX.
Output: Calibration/log read models, reusable calibration and logs modules, admin fixtures, routed section screens, and focused interaction tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-surface/06-CONTEXT.md
@.planning/phases/06-admin-surface/06-UI-SPEC.md
@.planning/phases/06-admin-surface/06-01-PLAN.md
@src/stores/admin/adminStore.ts
@src/stores/admin/selectors.ts
@src/stores/systemHealth/selectors.ts
@src/features/admin/readModels/dashboard.ts
@src/features/admin/AdminSectionRenderer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add calibration and logs read models plus local operational fixtures</name>
  <files>src/stores/admin/selectors.ts, src/mocks/admin/adminFixtures.ts, src/features/admin/readModels/calibration.ts, src/features/admin/readModels/logs.ts, src/features/admin/readModels/index.ts</files>
  <read_first>.planning/phases/06-admin-surface/06-UI-SPEC.md, src/stores/admin/selectors.ts, src/stores/systemHealth/selectors.ts, src/stores/admin/adminStore.ts</read_first>
  <action>Create `src/mocks/admin/adminFixtures.ts` with deterministic local calibration and log fixtures for tests and demo-safe screen verification. Extend `src/stores/admin/selectors.ts` with the minimal selector additions needed for the logs inspector path, including a selector that resolves the selected log entry from `selectedLogEntryId` and `logs`. Create `src/features/admin/readModels/calibration.ts` exporting `readCalibrationWorkspace()` and `readCalibrationSummaryCard()`. Create `src/features/admin/readModels/logs.ts` exporting `readLogWorkspace()` and `readLogSummaryCard()`. Update `src/features/admin/readModels/index.ts` to export the new read models. These files must compose existing selectors and normalized store state only; they must not import adapters, contracts, or runtime transport wrappers.</action>
  <verify>rg -n "readCalibrationWorkspace|readCalibrationSummaryCard|readLogWorkspace|readLogSummaryCard" src/features/admin/readModels/*.ts && rg -n "selectedLogEntryId|logs" src/stores/admin/selectors.ts && ! rg -n "@/adapters|contracts|WebSocket|fetch\\(" src/features/admin/readModels/*.ts src/stores/admin/selectors.ts</verify>
  <acceptance_criteria>
    - `src/mocks/admin/adminFixtures.ts` exports seeded calibration state and seeded log entries.
    - `src/stores/admin/selectors.ts` contains a selector that resolves the selected log detail from store state.
    - `src/features/admin/readModels/calibration.ts` and `logs.ts` export the exact read-model names from the action text.
    - None of the files in `<files>` import adapters, contract event types, or transport wrappers.
  </acceptance_criteria>
  <done>The calibration and logs workspaces have a stable read-model layer and deterministic local fixtures without broadening the runtime architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Build the calibration and logs workspace modules and route the sections in the admin renderer</name>
  <files>src/features/admin/calibration/CalibrationStatusPanel.tsx, src/features/admin/calibration/CalibrationChecklist.tsx, src/features/admin/calibration/CalibrationActionPanel.tsx, src/features/admin/logs/LogFilterBar.tsx, src/features/admin/logs/LogTable.tsx, src/features/admin/logs/LogInspectorDetail.tsx, src/features/admin/AdminSectionRenderer.tsx, src/screens/admin/CalibrationScreen.tsx, src/screens/admin/LogsScreen.tsx, src/screens/admin/index.ts</files>
  <read_first>.planning/phases/06-admin-surface/06-UI-SPEC.md, src/features/admin/readModels/calibration.ts, src/features/admin/readModels/logs.ts, src/features/admin/AdminSectionRenderer.tsx, src/components/primitives/index.ts</read_first>
  <action>Create the three calibration modules for status, prerequisite checklist, and start/cancel actions, following the UI-SPEC content order and copy rules. Create the three logs modules for filters, list/table rendering, and selected-log detail. Implement `src/screens/admin/CalibrationScreen.tsx` exporting `createAdminCalibrationScreenLayout()` and `src/screens/admin/LogsScreen.tsx` exporting `createAdminLogsScreenLayout()`, both using only the approved admin read models and store actions. Update `src/features/admin/AdminSectionRenderer.tsx` and `src/screens/admin/index.ts` so `calibration` and `logs` resolve to the new screen layouts without creating any new shell frame or route boundary.</action>
  <verify>rg -n "createAdminCalibrationScreenLayout|readCalibrationWorkspace|Start Calibration|Cancel Calibration" src/screens/admin/CalibrationScreen.tsx src/features/admin/calibration/*.tsx && rg -n "createAdminLogsScreenLayout|readLogWorkspace|Refresh Logs|LogInspectorDetail" src/screens/admin/LogsScreen.tsx src/features/admin/logs/*.tsx && rg -n "calibration|logs" src/features/admin/AdminSectionRenderer.tsx</verify>
  <acceptance_criteria>
    - `src/screens/admin/CalibrationScreen.tsx` contains `createAdminCalibrationScreenLayout` and consumes `readCalibrationWorkspace`.
    - `src/screens/admin/LogsScreen.tsx` contains `createAdminLogsScreenLayout` and consumes `readLogWorkspace`.
    - `src/features/admin/AdminSectionRenderer.tsx` routes both `calibration` and `logs` to their screen layout creators.
    - None of the new screen or feature files create a new shell frame, new nav system, or raw payload viewer.
  </acceptance_criteria>
  <done>The admin shell now contains real calibration and logs workspaces with inspector usage that stays inside the locked shell structure.</done>
</task>

<task type="auto">
  <name>Task 3: Add calibration and logs interaction coverage with operational-copy boundaries</name>
  <files>src/screens/admin/CalibrationScreen.test.tsx, src/screens/admin/LogsScreen.test.tsx</files>
  <read_first>src/screens/admin/CalibrationScreen.tsx, src/screens/admin/LogsScreen.tsx, src/mocks/admin/adminFixtures.ts</read_first>
  <action>Create `src/screens/admin/CalibrationScreen.test.tsx` covering current-status rendering, checklist visibility, start/cancel action visibility by state, and calm fallback messaging when prerequisites are not met. Create `src/screens/admin/LogsScreen.test.tsx` covering filtered log rendering, row selection, inspector detail rendering, and the boundary that log surfaces do not render raw JSON, stack traces, or transport labels by default.</action>
  <verify>npm run test -- --run src/screens/admin/CalibrationScreen.test.tsx src/screens/admin/LogsScreen.test.tsx</verify>
  <acceptance_criteria>
    - `src/screens/admin/CalibrationScreen.test.tsx` contains assertions for status, checklist, and start/cancel visibility.
    - `src/screens/admin/LogsScreen.test.tsx` contains assertions for row selection, inspector detail, and absence of raw debug/JSON presentation.
    - `npm run test -- --run src/screens/admin/CalibrationScreen.test.tsx src/screens/admin/LogsScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The calibration and logs sections are covered by focused interaction tests that enforce operational copy and inspector-detail boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/screens/admin/CalibrationScreen.test.tsx src/screens/admin/LogsScreen.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "createAdminCalibrationScreenLayout|createAdminLogsScreenLayout|readCalibrationWorkspace|readLogWorkspace" src/screens/admin/*.tsx src/features/admin/**/*.ts*`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The admin surface now includes real calibration and logs workspaces built on normalized store state and admin read models only
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-surface/06-02-SUMMARY.md`
</output>
