---
phase: 03-state-model-and-typed-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/stores/session/sessionStore.ts
  - src/stores/session/selectors.ts
  - src/stores/catalog/catalogStore.ts
  - src/stores/catalog/selectors.ts
  - src/stores/measurements/measurementsStore.ts
  - src/stores/measurements/selectors.ts
  - src/stores/fit/fitStore.ts
  - src/stores/fit/selectors.ts
  - src/stores/systemHealth/systemHealthStore.ts
  - src/stores/systemHealth/selectors.ts
  - src/stores/degraded/degradedStore.ts
  - src/stores/degraded/selectors.ts
  - src/stores/admin/adminStore.ts
  - src/stores/admin/selectors.ts
  - src/stores/uiMode/uiModeStore.ts
  - src/stores/uiMode/selectors.ts
  - src/lib/sessionReset/resetSession.ts
  - src/lib/sessionReset/resettableStores.ts
  - src/app/App.tsx
  - src/app/App.test.tsx
  - src/stores/selectors.test.ts
  - src/lib/sessionReset/resetSession.test.ts
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-04
user_setup: []
must_haves:
  truths:
    - Every Phase 3 store exposes colocated UI-safe selectors that return read models rather than raw inbound payloads or internal-only fields.
    - The reset coordinator clears all shopper-scoped state in a deterministic order while preserving catalog definitions, system health, admin operational state, and uiMode.
    - The root app consumes the `uiMode` store through typed selectors while leaving AppShell, ShopperShell, and AdminShell presentation-only.
  artifacts:
    - src/stores/session/selectors.ts
    - src/stores/catalog/selectors.ts
    - src/stores/measurements/selectors.ts
    - src/stores/fit/selectors.ts
    - src/stores/systemHealth/selectors.ts
    - src/stores/degraded/selectors.ts
    - src/stores/admin/selectors.ts
    - src/stores/uiMode/selectors.ts
    - src/lib/sessionReset/resetSession.ts
    - src/lib/sessionReset/resettableStores.ts
    - src/stores/selectors.test.ts
    - src/lib/sessionReset/resetSession.test.ts
    - src/app/App.tsx
    - src/app/App.test.tsx
  key_links:
    - Fit selectors expose qualitative confidence bands only, even though the fit store may retain an internal numeric confidence score.
    - Measurement selectors turn nullable measurement values into UI-safe display output without pretending missing values are present.
    - src/lib/sessionReset/resetSession.ts coordinates per-store reset hooks and is the only authority that completes `sessionEnd -> idle`.
---

<objective>
Add colocated selectors, deterministic reset orchestration, and the minimal app-level store consumption needed for Phase 3.

Purpose: Convert the base stores into a UI-safe state layer without adding feature screens, runtime behavior, or adapter code.
Output: Selector modules beside every store, a shared session reset coordinator, app-level uiMode store consumption, and tests for selectors and reset semantics.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTEXT.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/03-state-model-and-typed-contracts/03-01-PLAN.md
@src/app/App.tsx
@src/app/App.test.tsx
@src/stores/session/sessionStore.ts
@src/stores/catalog/catalogStore.ts
@src/stores/measurements/measurementsStore.ts
@src/stores/fit/fitStore.ts
@src/stores/systemHealth/systemHealthStore.ts
@src/stores/degraded/degradedStore.ts
@src/stores/admin/adminStore.ts
@src/stores/uiMode/uiModeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add colocated selector modules for every Phase 3 store with UI-safe read boundaries</name>
  <files>src/stores/session/selectors.ts, src/stores/catalog/selectors.ts, src/stores/measurements/selectors.ts, src/stores/fit/selectors.ts, src/stores/systemHealth/selectors.ts, src/stores/degraded/selectors.ts, src/stores/admin/selectors.ts, src/stores/uiMode/selectors.ts, src/stores/session/sessionStore.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/degraded/degradedStore.ts, src/stores/admin/adminStore.ts, src/stores/uiMode/uiModeStore.ts</files>
  <read_first>.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, src/stores/session/sessionStore.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/degraded/degradedStore.ts, src/stores/admin/adminStore.ts, src/stores/uiMode/uiModeStore.ts</read_first>
  <action>Create one `selectors.ts` file beside each store module. Implement the selector names locked in the contract spec, including `selectCanStartSession`, `selectSelectionReadyForTryOn`, `selectDisplayMeasurements`, `selectCurrentRecommendation`, `selectHealthSignals`, `selectActiveGuidance`, and the `uiMode` booleans. Enforce the locked planner choice that UI-facing selectors never expose `confidenceScore`; fit selectors may map internal numeric confidence to `low`, `medium`, or `high`, but they must return qualitative bands only. Measurement selectors must treat `null` values as unavailable and return only UI-safe display rows. Keep selector logic colocated with stores and free of React component imports or raw inbound envelope parsing.</action>
  <verify>rg -n \"selectCanStartSession|selectSelectionReadyForTryOn|selectDisplayMeasurements|selectCurrentRecommendation|selectUiMode|confidenceScore\" src/stores/**/selectors.ts</verify>
  <acceptance_criteria>
    - Every locked store directory contains a `selectors.ts` file.
    - No selector file exports a UI-facing accessor for `confidenceScore`.
    - `src/stores/measurements/selectors.ts` and `src/stores/fit/selectors.ts` explicitly handle nullable measurements and qualitative fit confidence.
  </acceptance_criteria>
  <done>The state layer exposes colocated selectors that are safe for shell and screen consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the centralized session reset coordinator and switch App to the uiMode store</name>
  <files>src/lib/sessionReset/resetSession.ts, src/lib/sessionReset/resettableStores.ts, src/stores/session/sessionStore.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/degraded/degradedStore.ts, src/stores/uiMode/uiModeStore.ts, src/app/App.tsx, src/app/App.test.tsx</files>
  <read_first>.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, src/app/App.tsx, src/app/App.test.tsx, src/stores/session/sessionStore.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/degraded/degradedStore.ts, src/stores/uiMode/uiModeStore.ts</read_first>
  <action>Create `src/lib/sessionReset/resettableStores.ts` as the single registry of per-store reset hooks for shopper-scoped domains and `src/lib/sessionReset/resetSession.ts` as the ordered coordinator that performs the locked reset sequence. Update the relevant stores to expose reset methods and any reset-pending helpers required by the coordinator. Do not clear catalog definitions, system health state, admin state, or uiMode during shopper reset. Replace the local `useState` demo state in `src/app/App.tsx` with the `uiMode` store and its colocated selectors while keeping `AppShell`, `ShopperShell`, and `AdminShell` presentation-only and prop-driven. Update `src/app/App.test.tsx` only as needed to preserve shell mode-switch coverage with store-backed state.</action>
  <verify>rg -n \"resetSession|resetPending|completeReset|setMode|selectUiMode\" src/lib/sessionReset/*.ts src/stores/**/*.ts src/app/App.tsx src/app/App.test.tsx</verify>
  <acceptance_criteria>
    - `src/lib/sessionReset/resetSession.ts` encodes the reset order from the contract spec.
    - `src/app/App.tsx` no longer uses local `useState` for shell mode and instead consumes the `uiMode` store.
    - `systemHealth`, `admin`, and `uiMode` are not registered as shopper-reset targets in `src/lib/sessionReset/resettableStores.ts`.
  </acceptance_criteria>
  <done>The app has a single deterministic shopper reset path and a real uiMode state container without changing the presentation shell contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for selector boundaries and reset semantics</name>
  <files>src/stores/selectors.test.ts, src/lib/sessionReset/resetSession.test.ts, src/app/App.test.tsx</files>
  <read_first>src/stores/**/selectors.ts, src/lib/sessionReset/resetSession.ts, src/app/App.tsx, src/app/App.test.tsx</read_first>
  <action>Create `src/stores/selectors.test.ts` to verify the highest-risk UI-safe selector behavior: session affordance booleans, catalog selection readiness, filtering of unavailable measurement values, qualitative fit confidence output, and `uiMode` booleans. Create `src/lib/sessionReset/resetSession.test.ts` to verify that shopper reset clears session, selection, measurement, fit, and shopper-scoped degraded state while preserving catalog definitions, system health, admin operational state, and uiMode. Keep `src/app/App.test.tsx` green under the store-backed uiMode path so the Phase 2 shell continues to mount through the Phase 3 state layer.</action>
  <verify>npm run test -- --run src/stores/session/sessionStore.test.ts src/stores/selectors.test.ts src/lib/sessionReset/resetSession.test.ts src/app/App.test.tsx</verify>
  <acceptance_criteria>
    - `src/stores/selectors.test.ts` contains explicit assertions that UI-facing fit selectors do not depend on numeric confidence values.
    - `src/lib/sessionReset/resetSession.test.ts` contains both clearing and persistence assertions.
    - `npm run test -- --run src/stores/session/sessionStore.test.ts src/stores/selectors.test.ts src/lib/sessionReset/resetSession.test.ts src/app/App.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Selectors and reset semantics are covered by focused automated tests before adapter contracts are introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/stores/session/sessionStore.test.ts src/stores/selectors.test.ts src/lib/sessionReset/resetSession.test.ts src/app/App.test.tsx`
- [ ] `npm run typecheck`
- [ ] `rg -n "selectors.ts" src/stores/*`
- [ ] `rg -n "resetSession|resetPending|completeReset" src/lib/sessionReset/*.ts src/stores/session/sessionStore.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 3 state layer exposes UI-safe selectors and a centralized deterministic shopper reset path without feature-screen work
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-model-and-typed-contracts/03-02-SUMMARY.md`
</output>
