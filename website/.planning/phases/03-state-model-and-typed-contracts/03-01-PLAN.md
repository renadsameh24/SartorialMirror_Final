---
phase: 03-state-model-and-typed-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/shared.ts
  - src/types/session.ts
  - src/types/catalog.ts
  - src/types/measurements.ts
  - src/types/fit.ts
  - src/types/system.ts
  - src/types/admin.ts
  - src/lib/session/sessionId.ts
  - src/stores/session/sessionStore.ts
  - src/stores/catalog/catalogStore.ts
  - src/stores/measurements/measurementsStore.ts
  - src/stores/fit/fitStore.ts
  - src/stores/systemHealth/systemHealthStore.ts
  - src/stores/degraded/degradedStore.ts
  - src/stores/admin/adminStore.ts
  - src/stores/uiMode/uiModeStore.ts
  - src/stores/session/sessionStore.test.ts
autonomous: true
requirements:
  - STATE-01
  - STATE-02
user_setup: []
must_haves:
  truths:
    - The codebase exposes canonical domain and shared utility types for Phase 3 without leaking runtime transport or UI implementation details into the type surface.
    - Every locked Phase 3 store exists as a typed Zustand module with clear ownership boundaries and no selector logic embedded in presentation components.
    - The shopper session machine is implemented as explicit typed transitions with frontend-created session IDs by default and runtime-provided session ID adoption support.
  artifacts:
    - src/types/shared.ts
    - src/types/session.ts
    - src/types/catalog.ts
    - src/types/measurements.ts
    - src/types/fit.ts
    - src/types/system.ts
    - src/types/admin.ts
    - src/lib/session/sessionId.ts
    - src/stores/session/sessionStore.ts
    - src/stores/catalog/catalogStore.ts
    - src/stores/measurements/measurementsStore.ts
    - src/stores/fit/fitStore.ts
    - src/stores/systemHealth/systemHealthStore.ts
    - src/stores/degraded/degradedStore.ts
    - src/stores/admin/adminStore.ts
    - src/stores/uiMode/uiModeStore.ts
    - src/stores/session/sessionStore.test.ts
  key_links:
    - src/types/measurements.ts encodes all canonical upper-body measurement keys while allowing individual `valueCm` values to be `null` until available.
    - src/types/fit.ts retains optional internal numeric confidence while keeping qualitative confidence bands available for later UI-safe selectors.
    - src/stores/session/sessionStore.ts uses helpers from src/lib/session/sessionId.ts to create frontend-default IDs and adopt runtime-provided IDs without changing the primary state-machine contract.
---

<objective>
Create the canonical Phase 3 domain types and the base typed Zustand store modules.

Purpose: Lock the implementation substrate for shopper state, domain state, and session identity before selectors, reset orchestration, and adapter contracts are added.
Output: Shared/domain type modules, base store modules for all locked state containers, a session ID helper, and a focused session-machine test.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTEXT.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md
@src/app/App.tsx
@src/app/shell/AppShell.tsx
@src/app/shell/ShopperShell.tsx
@src/app/shell/AdminShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical Phase 3 shared and domain type modules</name>
  <files>src/types/shared.ts, src/types/session.ts, src/types/catalog.ts, src/types/measurements.ts, src/types/fit.ts, src/types/system.ts, src/types/admin.ts</files>
  <read_first>AGENTS.md, docs/UI_SPEC.md, .planning/phases/03-state-model-and-typed-contracts/03-CONTEXT.md, .planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md</read_first>
  <action>Create the seven Phase 3 type modules exactly under `src/types/`. Put cross-domain primitives in `src/types/shared.ts`, shopper-machine and transition-facing types in `src/types/session.ts`, catalog entities and selection types in `src/types/catalog.ts`, measurement keys and snapshots in `src/types/measurements.ts`, fit types in `src/types/fit.ts`, health/guidance/degraded types in `src/types/system.ts`, and admin operational state in `src/types/admin.ts`. Encode the locked planner choice that all five canonical upper-body measurement keys exist in v1 while `valueCm` may be `null` until available. Encode the locked planner choice that fit types may retain `confidenceScore?: number` internally alongside qualitative `confidenceBand`. Do not add transport payload types, mock fixtures, feature-screen props, route enums, or backend-only fields such as price, inventory count, shopper profiles, or raw ML diagnostics.</action>
  <verify>rg -n \"type MeasurementKey|valueCm: number \\| null|confidenceScore\\?: number|type ShopperPhase|type UiMode\" src/types/*.ts</verify>
  <acceptance_criteria>
    - `src/types/measurements.ts` contains the exact keys `chest`, `waist`, `shoulderWidth`, `sleeveLength`, and `torsoLength`.
    - `src/types/measurements.ts` encodes `valueCm: number | null`.
    - `src/types/fit.ts` contains `confidenceBand` and `confidenceScore?: number`.
  </acceptance_criteria>
  <done>The Phase 3 type surface is explicit, local-first, upper-body scoped, and ready for store and contract imports.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the base typed Zustand stores and session ID helper</name>
  <files>src/lib/session/sessionId.ts, src/stores/session/sessionStore.ts, src/stores/catalog/catalogStore.ts, src/stores/measurements/measurementsStore.ts, src/stores/fit/fitStore.ts, src/stores/systemHealth/systemHealthStore.ts, src/stores/degraded/degradedStore.ts, src/stores/admin/adminStore.ts, src/stores/uiMode/uiModeStore.ts</files>
  <read_first>.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, src/types/shared.ts, src/types/session.ts, src/types/catalog.ts, src/types/measurements.ts, src/types/fit.ts, src/types/system.ts, src/types/admin.ts</read_first>
  <action>Create one Zustand module per locked store under `src/stores/`, using domain-specific directories and filenames exactly as listed in `<files>`. Each store should export its state type, its action type, and a `use...Store` hook plus an initial-state creator where useful. `src/stores/session/sessionStore.ts` must implement the explicit shopper machine with the locked primary states and transition API, enforce the allowed transition graph from the contract spec, create a frontend-default session ID through `src/lib/session/sessionId.ts`, and expose an action to adopt a runtime-provided session ID without changing machine ownership. The non-session stores should implement only owned state and write actions for their domains; do not add selectors yet. `src/stores/uiMode/uiModeStore.ts` must remain separate from shopper session state and expose only mode ownership actions. Do not import React components, shell primitives, adapter implementations, or runtime/WebSocket logic into any store.</action>
  <verify>rg -n \"create\\(|phase: 'idle'|startSession|markDetectionReady|confirmSelection|adoptRuntimeSessionId|setMode\" src/stores/**/*.ts src/lib/session/sessionId.ts</verify>
  <acceptance_criteria>
    - `src/stores/session/sessionStore.ts` contains actions for `startSession`, `markDetectionReady`, `confirmSelection`, `returnToCatalog`, `openFitDetails`, `returnToTryOn`, `endSession`, and `completeReset`.
    - `src/lib/session/sessionId.ts` supports frontend-created IDs by default and runtime-provided ID adoption helpers.
    - None of the store files import from `@/app`, `@/components`, `@/adapters` implementation files, or `react-router`.
  </acceptance_criteria>
  <done>Every Phase 3 store exists with clear ownership boundaries and the shopper machine is implemented in one explicit session store.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for the shopper state machine and session ID behavior</name>
  <files>src/stores/session/sessionStore.test.ts</files>
  <read_first>src/stores/session/sessionStore.ts, src/lib/session/sessionId.ts, .planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md</read_first>
  <action>Create `src/stores/session/sessionStore.test.ts` to verify the canonical transition path `idle -> detection -> catalog -> tryOn -> fitDetails -> tryOn -> sessionEnd`, early exits through `endSession`, and rejection or no-op behavior for invalid transitions such as confirming selection from `idle`. Add assertions that `startSession` generates a frontend-default session ID and that the store can adopt a runtime-provided session ID without leaving the locked machine shape. Keep this test file focused on the session machine only; selector behavior and reset coordinator semantics belong in later Phase 3 plans.</action>
  <verify>npm run test -- --run src/stores/session/sessionStore.test.ts</verify>
  <acceptance_criteria>
    - `src/stores/session/sessionStore.test.ts` contains a full happy-path transition test and at least one invalid-transition guard test.
    - The test suite asserts frontend-default session ID creation and runtime session ID adoption.
    - `npm run test -- --run src/stores/session/sessionStore.test.ts` exits 0.
  </acceptance_criteria>
  <done>The shopper state machine is covered by an automated test before selectors, reset orchestration, or ports build on top of it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/stores/session/sessionStore.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "type MeasurementKey|valueCm: number \\| null|confidenceScore\\?: number" src/types/*.ts`
- [ ] `rg -n "startSession|markDetectionReady|confirmSelection|adoptRuntimeSessionId" src/stores/session/sessionStore.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a locked Phase 3 type surface and base store modules without leaking Phase 4 or Phase 5 behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-model-and-typed-contracts/03-01-SUMMARY.md`
</output>
