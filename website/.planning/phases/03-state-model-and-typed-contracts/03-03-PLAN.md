---
phase: 03-state-model-and-typed-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/adapters/contracts/shared.ts
  - src/adapters/contracts/runtime.ts
  - src/adapters/contracts/unity.ts
  - src/adapters/contracts/catalog.ts
  - src/adapters/contracts/commands.ts
  - src/adapters/contracts/ports.ts
  - src/adapters/contracts/index.ts
  - src/adapters/contracts/contracts.test.ts
autonomous: true
requirements:
  - STATE-03
user_setup: []
must_haves:
  truths:
    - The repository defines one shared inbound event envelope with the exact locked fields `type`, `source`, `timestamp`, `sessionId?`, and `payload`.
    - Runtime, Unity, and catalog contracts exist as separate typed event and port seams with no transport implementation code.
    - Shopper and admin intents are expressed as typed command unions and consumed only through `RuntimePort`, `UnityPort`, and `CatalogPort`.
  artifacts:
    - src/adapters/contracts/shared.ts
    - src/adapters/contracts/runtime.ts
    - src/adapters/contracts/unity.ts
    - src/adapters/contracts/catalog.ts
    - src/adapters/contracts/commands.ts
    - src/adapters/contracts/ports.ts
    - src/adapters/contracts/index.ts
    - src/adapters/contracts/contracts.test.ts
  key_links:
    - src/adapters/contracts/shared.ts provides the canonical envelope and base command types reused by every source-specific contract.
    - src/adapters/contracts/ports.ts restricts each port to the relevant event and command subsets without leaking WebSocket, Unity bridge, or local catalog implementation details.
    - src/adapters/contracts/contracts.test.ts proves the locked event, command, and port surfaces compile and remain implementation-free.
---

<objective>
Define the typed event, command, and port contract layer for future runtime, Unity, and catalog integrations.

Purpose: Finish the Phase 3 integration seams so later phases can plug in adapters without rewriting stores or presentation.
Output: Source-specific inbound event unions, outbound command unions, typed port interfaces, and focused contract tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTEXT.md
@.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md
@.planning/phases/03-state-model-and-typed-contracts/03-01-PLAN.md
@src/types/shared.ts
@src/types/catalog.ts
@src/types/measurements.ts
@src/types/fit.ts
@src/types/system.ts
@src/types/admin.ts
@src/types/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared inbound envelope and source-specific inbound event modules</name>
  <files>src/adapters/contracts/shared.ts, src/adapters/contracts/runtime.ts, src/adapters/contracts/unity.ts, src/adapters/contracts/catalog.ts</files>
  <read_first>.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, src/types/shared.ts, src/types/catalog.ts, src/types/measurements.ts, src/types/fit.ts, src/types/system.ts</read_first>
  <action>Create `src/adapters/contracts/shared.ts` with the canonical `InboundEventEnvelope` and base source/type primitives. Create `runtime.ts`, `unity.ts`, and `catalog.ts` with separate event unions exactly aligned to the contract spec. Use the locked shared envelope fields `type`, `source`, `timestamp`, `sessionId?`, and `payload`. Keep `sessionId` optional to support both frontend-created and runtime-provided session identity paths. Reuse the normalized domain types from `src/types/` for event payloads; do not add WebSocket payload shapes, JSON parser helpers, runtime mode branches, or implementation-specific event listeners.</action>
  <verify>rg -n \"InboundEventEnvelope|runtime\\.|unity\\.|catalog\\.snapshot|sessionId\\?: SessionId\" src/adapters/contracts/*.ts</verify>
  <acceptance_criteria>
    - `src/adapters/contracts/shared.ts` contains the shared envelope with the exact locked field names.
    - `src/adapters/contracts/runtime.ts`, `unity.ts`, and `catalog.ts` each export a source-specific event union.
    - None of the contract files import `WebSocket`, `window`, `fetch`, `postMessage`, or runtime implementation helpers.
  </acceptance_criteria>
  <done>The repo has a normalized inbound contract surface split cleanly by source.</done>
</task>

<task type="auto">
  <name>Task 2: Define shopper/admin command unions and the three locked port interfaces</name>
  <files>src/adapters/contracts/commands.ts, src/adapters/contracts/ports.ts, src/adapters/contracts/index.ts</files>
  <read_first>.planning/phases/03-state-model-and-typed-contracts/03-CONTRACT-SPEC.md, src/adapters/contracts/shared.ts, src/adapters/contracts/runtime.ts, src/adapters/contracts/unity.ts, src/adapters/contracts/catalog.ts, src/types/session.ts, src/types/catalog.ts, src/types/admin.ts</read_first>
  <action>Create `src/adapters/contracts/commands.ts` with the base command type plus the exact shopper and admin intent unions from the contract spec. Create `src/adapters/contracts/ports.ts` with `RuntimePort`, `UnityPort`, and `CatalogPort` only. Lock `CatalogPort` to the required `loadSnapshot()` method plus the optional `subscribe()` surface now. Restrict each port’s `send()` signature to the relevant command subset instead of accepting the full command union. Export the complete contract surface from `src/adapters/contracts/index.ts`. Do not add adapter classes, mock implementations, environment branching, event emitter wrappers, or transport config objects.</action>
  <verify>rg -n \"interface RuntimePort|interface UnityPort|interface CatalogPort|shopper\\.session\\.start|admin\\.calibration\\.start|subscribe\\?\" src/adapters/contracts/*.ts</verify>
  <acceptance_criteria>
    - `src/adapters/contracts/ports.ts` exports exactly `RuntimePort`, `UnityPort`, and `CatalogPort`.
    - `CatalogPort` contains both `loadSnapshot()` and optional `subscribe()`.
    - `src/adapters/contracts/commands.ts` contains the locked shopper and admin command names and no implementation code.
  </acceptance_criteria>
  <done>The contract layer exposes the exact command and port surfaces Phase 4 will implement against.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused compile-time contract tests and guard against accidental implementation leakage</name>
  <files>src/adapters/contracts/contracts.test.ts</files>
  <read_first>src/adapters/contracts/shared.ts, src/adapters/contracts/runtime.ts, src/adapters/contracts/unity.ts, src/adapters/contracts/catalog.ts, src/adapters/contracts/commands.ts, src/adapters/contracts/ports.ts</read_first>
  <action>Create `src/adapters/contracts/contracts.test.ts` using Vitest `expectTypeOf` assertions to verify the shared event envelope field names, the presence of the source-specific unions, the optional `CatalogPort.subscribe` surface, the relevant command subsets accepted by `RuntimePort` and `UnityPort`, and the absence of a UI-facing requirement for numeric fit confidence in contract-level read surfaces. Add one grep-guard verification in the plan to ensure the contract layer remains implementation-free and does not import `WebSocket`, `window`, `fetch`, or local mock code.</action>
  <verify>npm run test -- --run src/adapters/contracts/contracts.test.ts && ! rg -n \"WebSocket|fetch\\(|postMessage|window\\.|mock\" src/adapters/contracts/*.ts</verify>
  <acceptance_criteria>
    - `src/adapters/contracts/contracts.test.ts` contains `expectTypeOf` checks for the envelope, ports, and command subsets.
    - The contract test passes without requiring any runtime adapter implementation.
    - The grep guard finds no transport or mock implementation leakage under `src/adapters/contracts/`.
  </acceptance_criteria>
  <done>The event/command/port contract layer is compile-verified and remains pure type surface only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/adapters/contracts/contracts.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "interface RuntimePort|interface UnityPort|interface CatalogPort|subscribe\\?" src/adapters/contracts/*.ts`
- [ ] `! rg -n "WebSocket|fetch\\(|postMessage|window\\.|mock" src/adapters/contracts/*.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 3 contract layer is fully typed and implementation-free, ready for Phase 4 adapters
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-model-and-typed-contracts/03-03-SUMMARY.md`
</output>
