---
phase: 01-foundation-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/main.tsx
  - src/vite-env.d.ts
  - src/app/App.tsx
  - src/app/shell/AppShell.tsx
  - src/app/shell/shellMode.ts
  - src/styles/globals.css
  - src/styles/tokens.css
  - src/app/App.test.tsx
  - src/screens/.gitkeep
  - src/components/.gitkeep
  - src/features/.gitkeep
  - src/stores/.gitkeep
  - src/adapters/.gitkeep
  - src/lib/.gitkeep
  - src/hooks/.gitkeep
  - src/types/.gitkeep
  - src/mocks/.gitkeep
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - The repository contains the planned Phase 1 source tree with clean seams for later work.
    - The app boots into a minimal themed shell that can render shopper and admin visual modes without implementing feature screens.
    - System-level tokens and reduced-motion styling exist before Phase 2 app-shell design work begins.
  artifacts:
    - src/main.tsx
    - src/vite-env.d.ts
    - src/app/App.tsx
    - src/app/shell/AppShell.tsx
    - src/app/shell/shellMode.ts
    - src/styles/globals.css
    - src/styles/tokens.css
    - src/app/App.test.tsx
    - src/screens/.gitkeep
    - src/components/.gitkeep
    - src/features/.gitkeep
    - src/stores/.gitkeep
    - src/adapters/.gitkeep
    - src/lib/.gitkeep
    - src/hooks/.gitkeep
    - src/types/.gitkeep
    - src/mocks/.gitkeep
  key_links:
    - src/main.tsx mounts App into the index.html root element created in Plan 01.
    - App renders AppShell using a local shell-mode switch that proves shopper/admin theme separation without domain logic.
    - globals.css loads tokens.css and applies reduced-motion behavior at the system layer.
---

<objective>
Create the source scaffold, token foundation, and minimal themed shell placeholder for the Phase 1 repository.

Purpose: Prove the app can boot with the locked visual foundation and directory structure before feature work begins.
Output: `src/` scaffold, token CSS, minimal shell, and a smoke test.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bootstrap/01-CONTEXT.md
@.planning/phases/01-foundation-bootstrap/01-UI-SPEC.md
@package.json
@vite.config.ts
@tailwind.config.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the planned src directory scaffold and entrypoint placeholders</name>
  <files>src/vite-env.d.ts, src/screens/.gitkeep, src/components/.gitkeep, src/features/.gitkeep, src/stores/.gitkeep, src/adapters/.gitkeep, src/lib/.gitkeep, src/hooks/.gitkeep, src/types/.gitkeep, src/mocks/.gitkeep</files>
  <read_first>.planning/phases/01-foundation-bootstrap/01-CONTEXT.md, .planning/ROADMAP.md</read_first>
  <action>Create the Phase 1 source layout exactly as locked in D-16 through D-19. Add `.gitkeep` files for empty top-level directories that must exist now but will not contain implementation yet. Add `src/vite-env.d.ts` for the Vite/TypeScript environment. Do not create feature-specific components, adapters, or domain stores inside these directories yet; the purpose is structural seams only.</action>
  <verify>test -f src/vite-env.d.ts && test -f src/screens/.gitkeep && test -f src/components/.gitkeep && test -f src/adapters/.gitkeep && test -f src/mocks/.gitkeep</verify>
  <acceptance_criteria>
    - All locked top-level source directories exist.
    - Empty directories are preserved in git via `.gitkeep`.
    - No feature-specific implementation is added to deferred directories.
  </acceptance_criteria>
  <done>The source tree matches the planned architecture without leaking later-phase work into Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the token CSS foundation and reduced-motion global styles</name>
  <files>src/styles/tokens.css, src/styles/globals.css</files>
  <read_first>.planning/phases/01-foundation-bootstrap/01-UI-SPEC.md, tailwind.config.ts</read_first>
  <action>Create `src/styles/tokens.css` as the source of truth for CSS variables using separate `shopper-*` and `admin-*` token namespaces plus shared semantic aliases. Encode the locked spacing scale, typography roles, color roles, focus token, radius, shadow, and motion tokens there. Create `src/styles/globals.css` to load Tailwind layers, import the token file, define base document/body behavior, set up shell-safe defaults, and implement reduced-motion handling via `prefers-reduced-motion`. Do not design feature screens or add catalog/try-on/admin panels here.</action>
  <verify>test -f src/styles/tokens.css && test -f src/styles/globals.css && rg -- '--shopper-' src/styles/tokens.css && rg -- '--admin-' src/styles/tokens.css && rg 'prefers-reduced-motion' src/styles/globals.css</verify>
  <acceptance_criteria>
    - Shopper and admin token namespaces both exist.
    - Locked spacing/type tokens from the UI contract are represented in CSS variables.
    - Reduced-motion rules exist at the system layer.
  </acceptance_criteria>
  <done>The repo has the token and global style foundation required before Phase 2 shell design work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the minimal themed shell placeholder and smoke-test it</name>
  <files>src/main.tsx, src/app/App.tsx, src/app/shell/AppShell.tsx, src/app/shell/shellMode.ts, src/app/App.test.tsx</files>
  <read_first>index.html, .planning/phases/01-foundation-bootstrap/01-CONTEXT.md, .planning/phases/01-foundation-bootstrap/01-UI-SPEC.md, vitest.setup.ts</read_first>
  <behavior>
    - Test 1: Rendering `App` mounts a minimal shell successfully with foundation placeholder content.
    - Test 2: The shell can render shopper and admin visual modes without importing router, domain state, or feature screens.
    - Test 3: The placeholder exposes the expected shell landmarks/theme hooks needed for later phases.
  </behavior>
  <action>Create `src/main.tsx` that imports global styles and mounts the app into `#root`. Create `src/app/App.tsx` that renders a minimal root shell and proves mode switching with local shell-level state only; do not introduce domain state logic. Create `src/app/shell/shellMode.ts` with the shared shell mode type. Create `src/app/shell/AppShell.tsx` with a neutral placeholder layout that demonstrates shopper/admin theme separation and the stage/rail shell zones from the UI contract without becoming a real screen. Add `src/app/App.test.tsx` as a smoke test using React Testing Library. Keep all placeholder copy neutral and foundation-only.</action>
  <verify>npm run typecheck && npm run test -- --run src/app/App.test.tsx && npm run build</verify>
  <acceptance_criteria>
    - The app mounts through `src/main.tsx` without unresolved imports.
    - A minimal shell renders and can switch visual mode at the shell layer.
    - The smoke test passes and the project builds successfully.
  </acceptance_criteria>
  <done>The scaffold boots into a minimal themed shell and proves the Phase 1 foundation works end-to-end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run test -- --run src/app/App.test.tsx`
- [ ] `npm run build`
- [ ] `test -f src/main.tsx && test -f src/styles/tokens.css && test -f src/app/shell/AppShell.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a bootable, token-driven foundation shell and the locked Phase 1 source structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bootstrap/01-03-SUMMARY.md`
</output>
