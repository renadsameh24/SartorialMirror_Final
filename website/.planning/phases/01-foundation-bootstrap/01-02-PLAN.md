---
phase: 01-foundation-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - vitest.setup.ts
  - tailwind.config.ts
  - postcss.config.cjs
  - eslint.config.js
  - .prettierrc.json
  - .prettierignore
autonomous: true
requirements:
  - FOUND-02
user_setup: []
must_haves:
  truths:
    - TypeScript, Vite, Tailwind/PostCSS, ESLint, Prettier, and Vitest are configured for the locked stack before feature code exists.
    - Configurations share a stable `@/*` alias and a jsdom-based test environment.
    - Tailwind is prepared to consume semantic CSS variable tokens instead of hardcoded feature colors.
  artifacts:
    - tsconfig.json
    - tsconfig.app.json
    - tsconfig.node.json
    - vite.config.ts
    - vitest.config.ts
    - vitest.setup.ts
    - tailwind.config.ts
    - postcss.config.cjs
    - eslint.config.js
    - .prettierrc.json
    - .prettierignore
  key_links:
    - tsconfig, Vite, and Vitest use the same `@/*` to `src/*` alias.
    - Tailwind theme extension maps semantic design tokens, spacing, and type values from the approved UI contract.
    - Vitest setup loads Testing Library and jest-dom through a dedicated setup file.
---

<objective>
Create the full Phase 1 configuration layer for building, styling, linting, formatting, and testing.

Purpose: Lock the toolchain before any src implementation work begins.
Output: TypeScript, Vite, Vitest, Tailwind/PostCSS, ESLint, and Prettier config files.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bootstrap/01-CONTEXT.md
@.planning/phases/01-foundation-bootstrap/01-UI-SPEC.md
@package.json
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure TypeScript, Vite, and Vitest with a shared alias baseline</name>
  <files>tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, vitest.config.ts</files>
  <read_first>package.json, AGENTS.md, .planning/phases/01-foundation-bootstrap/01-CONTEXT.md</read_first>
  <action>Create strict TypeScript configs suitable for a Vite React app, including a root `tsconfig.json` plus app/node splits if needed per D-13 and D-19. Add the `@/*` alias mapping to `src/*` in TypeScript, Vite, and Vitest. Configure `vite.config.ts` for the React plugin only; do not introduce router, SSR, or backend integration concerns. Configure `vitest.config.ts` for `jsdom`, path alias resolution, and a dedicated setup file. Keep the configuration single-app and local-first.</action>
  <verify>test -f tsconfig.app.json && test -f vite.config.ts && test -f vitest.config.ts && rg '"@/\\*"' tsconfig.app.json && rg "'@'" vite.config.ts && rg "jsdom" vitest.config.ts</verify>
  <acceptance_criteria>
    - TypeScript configs are strict and app-focused.
    - `@/*` alias is declared consistently in TypeScript, Vite, and Vitest.
    - Vitest is configured for jsdom and references a setup file.
  </acceptance_criteria>
  <done>The core compile/build/test config layer exists and is internally consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind and PostCSS around semantic foundation tokens</name>
  <files>tailwind.config.ts, postcss.config.cjs</files>
  <read_first>.planning/phases/01-foundation-bootstrap/01-UI-SPEC.md, AGENTS.md</read_first>
  <action>Create `tailwind.config.ts` with content globs for `index.html` and `src/**/*.{ts,tsx}`. Extend the theme with semantic token hooks that expect CSS variables for surface, text, border, accent, destructive, focus, radius, shadow, and motion values. Lock the spacing scale to `4, 8, 16, 24, 32, 48, 64` and the type scale to label `16`, body `18`, heading `32`, display `56`, with only weights `400` and `600`. Create `postcss.config.cjs` with the Tailwind and Autoprefixer pipeline only.</action>
  <verify>test -f tailwind.config.ts && test -f postcss.config.cjs && rg "16|24|32|48|64" tailwind.config.ts && rg "var\\(--" tailwind.config.ts && rg "autoprefixer" postcss.config.cjs</verify>
  <acceptance_criteria>
    - Tailwind consumes semantic token aliases instead of feature-specific raw values.
    - The locked spacing and typography scales from `01-UI-SPEC.md` appear in the config.
    - PostCSS is configured only for the Tailwind pipeline.
  </acceptance_criteria>
  <done>The styling config is ready for a CSS-variable token layer without designing feature UI yet.</done>
</task>

<task type="auto">
  <name>Task 3: Configure linting, formatting, and the test setup baseline</name>
  <files>eslint.config.js, .prettierrc.json, .prettierignore, vitest.setup.ts</files>
  <read_first>AGENTS.md, .planning/phases/01-foundation-bootstrap/01-CONTEXT.md, package.json</read_first>
  <action>Create `eslint.config.js` using flat config for browser-based React + TypeScript work, including `@eslint/js`, `typescript-eslint`, `react-hooks`, and `react-refresh` guidance. Create `.prettierrc.json` and `.prettierignore` for a minimal repo-wide formatting contract. Create `vitest.setup.ts` that imports `@testing-library/jest-dom` and any minimal cleanup/setup needed for React Testing Library. Do not add pre-commit hooks, CI integration, or opinionated extra lint plugins beyond the locked baseline.</action>
  <verify>test -f eslint.config.js && test -f .prettierrc.json && test -f vitest.setup.ts && rg "react-hooks" eslint.config.js && rg "@testing-library/jest-dom" vitest.setup.ts && npm exec eslint --version</verify>
  <acceptance_criteria>
    - ESLint config exists for TS + React with hooks support.
    - Prettier config and ignore file exist.
    - Vitest setup loads jest-dom for RTL assertions.
  </acceptance_criteria>
  <done>The repo has a local-first quality baseline ready for src implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm exec eslint --version`
- [ ] `npm exec vitest --version`
- [ ] `test -f tsconfig.json && test -f vite.config.ts && test -f tailwind.config.ts && test -f eslint.config.js && test -f .prettierrc.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has the locked Phase 1 config baseline for TypeScript, Vite, styling, linting, formatting, and tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bootstrap/01-02-SUMMARY.md`
</output>
