---
phase: 01-foundation-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - index.html
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - Developers can install the project with npm and have a complete dependency lockfile.
    - The repository exposes root scripts for local development, build, lint, format, typecheck, preview, and test workflows.
    - The app has a Vite HTML entry point and the repo ignores transient dependency and build artifacts.
  artifacts:
    - package.json
    - package-lock.json
    - .gitignore
    - index.html
  key_links:
    - package.json scripts reference locally installed Vite, TypeScript, ESLint, Prettier, and Vitest tooling from package-lock.json.
    - index.html exposes a root mount node that the later src/main.tsx entry will hydrate.
    - .gitignore excludes node_modules, dist, coverage, and local OS/editor artifacts.
---

<objective>
Create the repository-level npm and HTML bootstrap for the greenfield front-end.

Purpose: Establish the minimum runnable project metadata before configuration and source scaffolding begin.
Output: Root manifest, lockfile, gitignore, and Vite entry HTML.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bootstrap/01-CONTEXT.md
@.planning/phases/01-foundation-bootstrap/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the npm package manifest for the locked Phase 1 stack</name>
  <files>package.json</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-bootstrap/01-CONTEXT.md, .planning/phases/01-foundation-bootstrap/01-UI-SPEC.md</read_first>
  <action>Create `package.json` for a single-package frontend app per D-04 through D-09. Set the package to private, use npm, and add scripts for `dev`, `build`, `preview`, `typecheck`, `lint`, `format`, `format:check`, `test`, and `test:watch`. Add runtime dependencies for `react`, `react-dom`, `zustand`, `framer-motion`, and `lucide-react`. Add dev dependencies for the locked toolchain only: TypeScript, Vite, React plugin, React type packages, Tailwind CSS, PostCSS, Autoprefixer, ESLint flat-config dependencies, Prettier, Vitest, jsdom, and React Testing Library packages. Do not add React Router, Storybook, Playwright, Electron, CI helpers, or backend/integration libraries.</action>
  <verify>test -f package.json && rg '"dev"' package.json && rg '"typecheck"' package.json && rg '"react"' package.json && rg '"vite"' package.json</verify>
  <acceptance_criteria>
    - `package.json` contains the locked stack dependencies and no router/e2e/storybook/electron packages.
    - `package.json` exposes scripts for dev, build, preview, typecheck, lint, format, format:check, test, and test:watch.
  </acceptance_criteria>
  <done>`package.json` fully describes the Phase 1 dependency and script baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and materialize the npm lockfile</name>
  <files>package-lock.json</files>
  <read_first>package.json</read_first>
  <action>Run `npm install` from the repo root to create `package-lock.json` and verify the project is actually installable with npm per D-05 and FOUND-01. Do not add optional tooling outside the locked stack during installation. If npm modifies `package.json` metadata unexpectedly, normalize it back to the intended manifest before finishing.</action>
  <verify>test -f package-lock.json && npm ls react react-dom vite eslint prettier vitest --depth=0</verify>
  <acceptance_criteria>
    - `package-lock.json` exists and matches the declared dependencies.
    - `npm ls` resolves the primary stack and toolchain packages without missing dependencies.
  </acceptance_criteria>
  <done>The repository installs cleanly with npm and has a committed lockfile.</done>
</task>

<task type="auto">
  <name>Task 3: Add repository ignore rules and root HTML entry</name>
  <files>.gitignore, index.html</files>
  <read_first>AGENTS.md, .planning/phases/01-foundation-bootstrap/01-CONTEXT.md</read_first>
  <action>Create `.gitignore` with only the standard local frontend exclusions needed for this repo: `node_modules`, `dist`, `coverage`, Vite cache output, and local OS/editor junk such as `.DS_Store`. Create `index.html` for the Vite app with a single `#root` mount node, responsive viewport meta, and a neutral document title suitable for a foundation placeholder. Keep the HTML minimal and free of shopper/admin feature markup.</action>
  <verify>test -f .gitignore && test -f index.html && rg '^node_modules/?$' .gitignore && rg 'id="root"' index.html && rg 'viewport' index.html</verify>
  <acceptance_criteria>
    - `.gitignore` excludes dependency, build, coverage, and local machine artifacts.
    - `index.html` contains a root mount element and standard viewport metadata only.
  </acceptance_criteria>
  <done>The repo has the root files required for a clean Vite bootstrap.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` completes successfully
- [ ] `npm ls react react-dom vite eslint prettier vitest --depth=0` succeeds
- [ ] `test -f package.json && test -f package-lock.json && test -f .gitignore && test -f index.html`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can be installed with npm and has the root bootstrap files needed for later configuration and source scaffolding
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bootstrap/01-01-SUMMARY.md`
</output>
