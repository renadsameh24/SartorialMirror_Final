---
phase: 02-app-shell-and-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tailwind.config.ts
  - src/styles/tokens.css
  - src/styles/globals.css
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
user_setup: []
must_haves:
  truths:
    - The token system exposes the Phase 2 semantic additions for shell depth, border depth, focus layering, region sizing, control sizing, and shell-only motion.
    - Global shell utilities encode the locked kiosk safe areas, width ratios, and low-resolution fallback behavior for shopper and admin modes.
    - Shared focus styling is token-driven and consistent across shell and primitive work.
  artifacts:
    - src/styles/tokens.css
    - src/styles/globals.css
    - tailwind.config.ts
  key_links:
    - src/styles/tokens.css defines the semantic source of truth and mode-specific values for shopper and admin shell sizing and surface behavior.
    - src/styles/globals.css turns the token set into shell classes, overlay lanes, focus ring layering, and compact-width fallback behavior.
    - tailwind.config.ts exposes the semantic CSS variables so JSX can keep using semantic utilities rather than raw values.
---

<objective>
Expand the Phase 1 theme baseline into the full Phase 2 shell and primitive token system.

Purpose: Lock the implementation-facing style contract before generic primitive work and shell polish.
Output: Semantic token expansion, global shell utility updates, and Tailwind semantic mappings for the new shell contract.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-and-design-system/02-CONTEXT.md
@.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md
@src/styles/tokens.css
@src/styles/globals.css
@tailwind.config.ts
@src/app/shell/ShopperShell.tsx
@src/app/shell/AdminShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand src/styles/tokens.css with the Phase 2 semantic shell and primitive token set</name>
  <files>src/styles/tokens.css</files>
  <read_first>.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md, src/styles/tokens.css, tailwind.config.ts</read_first>
  <action>Keep the Phase 1 token names intact and add the Phase 2 semantic tokens from the approved UI contract. Add the exact token families `--color-surface-overlay`, `--color-surface-ghost`, `--color-surface-strong`, `--color-text-secondary`, `--color-text-inverse`, `--color-border-subtle`, `--color-border-strong`, `--color-accent-contrast`, `--color-focus-ring-inner`, `--color-focus-ring-outer`, `--focus-ring-width-inner`, `--focus-ring-width-outer`, `--shell-frame-pad-inline`, `--shell-frame-pad-block`, `--shell-band-height`, `--shell-gap`, `--shopper-stage-safe-inset`, `--shopper-context-rail-width`, `--shopper-overlay-lane-height`, `--admin-nav-rail-width`, `--admin-inspector-width`, `--admin-command-bar-height`, `--control-min-height`, `--control-pad-inline`, `--radius-badge`, `--radius-overlay`, `--shadow-overlay`, `--shadow-float`, `--motion-shell-enter`, `--motion-shell-exit`, `--motion-overlay-enter`, `--motion-overlay-exit`, and `--motion-easing-emphasized`. Use concrete values aligned to the UI contract: `--focus-ring-width-inner: 2px`, `--focus-ring-width-outer: 4px`, `--control-min-height: 48px`, `--control-pad-inline: 16px`, `--radius-badge: 999px`, `--radius-overlay: 20px`, shopper context rail width `clamp(320px, 18vw, 384px)`, shopper stage safe inset `48px`, shopper overlay lane height `88px`, admin nav rail width `clamp(240px, 15vw, 288px)`, admin inspector width `clamp(280px, 18vw, 360px)`, and admin command bar height `80px`. Alias the shared semantic names under both `[data-shell-mode='shopper']` and `[data-shell-mode='admin']` so mode-specific values stay behind the same semantic interface.</action>
  <verify>rg -n -- "--color-surface-overlay|--shell-band-height|--shopper-context-rail-width|--admin-nav-rail-width|--control-min-height|--motion-shell-enter" src/styles/tokens.css</verify>
  <acceptance_criteria>
    - `src/styles/tokens.css` contains every Phase 2 token named in the action text.
    - `src/styles/tokens.css` contains `clamp(320px, 18vw, 384px)` and `clamp(240px, 15vw, 288px)`.
    - Both `[data-shell-mode='shopper']` and `[data-shell-mode='admin']` assign values for `--shell-band-height`, `--shopper-context-rail-width` or `--admin-nav-rail-width`, and the focus-ring tokens.
  </acceptance_criteria>
  <done>The token file exposes the full semantic contract needed for shell layout, focus treatment, primitives, and shell-only motion.</done>
</task>

<task type="auto">
  <name>Task 2: Update global shell utilities, focus layering, and fallback layout classes</name>
  <files>src/styles/globals.css</files>
  <read_first>src/styles/globals.css, src/styles/tokens.css, .planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md</read_first>
  <action>Extend src/styles/globals.css with the concrete shell utilities required by the Phase 2 contract. Add classes for the top shell band, shopper shell row, admin shell row, protected stage viewport, right context rail, admin nav rail, workspace canvas, inspector rail, and overlay lane positioning. Encode the locked width behavior using CSS grid and token-backed sizing: shopper shell row uses stage plus right rail; admin shell row uses left nav, workspace, and optional inspector. Add layered focus-ring utilities that use `--color-focus-ring-inner`, `--color-focus-ring-outer`, `--focus-ring-width-inner`, and `--focus-ring-width-outer`. Add low-resolution breakpoints at `1440px` and `1280px` so the shopper right rail stacks below the stage and the admin inspector stacks below the workspace while the admin nav rail remains structurally visible. Keep reduced-motion handling in place and extend it only for shell transition classes; do not add feature-state motion.</action>
  <verify>rg -n "shopper-shell-row|admin-shell-row|stage-viewport|context-rail|admin-nav-rail|inspector-rail|overlay-lane|@media \\(max-width: 1280px\\)|focus-ring" src/styles/globals.css</verify>
  <acceptance_criteria>
    - `src/styles/globals.css` contains class definitions for `shopper-shell-row`, `admin-shell-row`, `stage-viewport`, `context-rail`, `admin-nav-rail`, and `overlay-lane`.
    - `src/styles/globals.css` contains `@media (max-width: 1440px)` and `@media (max-width: 1280px)`.
    - The focus-visible styling in `src/styles/globals.css` references the new layered focus tokens instead of a single hard-coded outer ring only.
  </acceptance_criteria>
  <done>The global style layer directly encodes the locked shell geometry, fallback behavior, and focus system needed by Phase 2 components.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the new semantic style hooks through Tailwind config and verify style contract stability</name>
  <files>tailwind.config.ts, src/styles/tokens.css, src/styles/globals.css</files>
  <read_first>tailwind.config.ts, src/styles/tokens.css, src/styles/globals.css</read_first>
  <action>Extend tailwind.config.ts so JSX can keep using semantic utility classes instead of raw values. Add the new surface, text, and border aliases under `colors`, add `badge` and `overlay` under `borderRadius`, add `overlay` and `float` under `boxShadow`, and expose token-backed values for control min-height and any transition durations needed by shell motion. Keep the existing semantic naming style and do not add raw color literals or dashboard-oriented token names. After the config update, verify the style contract by running typecheck and build, because JSX and CSS token names must remain consistent across shell and primitive work.</action>
  <verify>npm run typecheck && npm run build && rg -n "surface:|text:|border:|badge|overlay|float" tailwind.config.ts</verify>
  <acceptance_criteria>
    - `tailwind.config.ts` contains semantic aliases for the new surface, text, and border roles and does not contain new raw hex literals.
    - `tailwind.config.ts` contains `badge` and `overlay` radius entries and `overlay` and `float` shadow entries.
    - `npm run typecheck` and `npm run build` exit 0 after the token and global style updates.
  </acceptance_criteria>
  <done>Tailwind stays aligned with the expanded token contract and the shell style layer compiles cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n -- "--shell-band-height|--shopper-context-rail-width|--admin-nav-rail-width|--control-min-height" src/styles/tokens.css`
- [ ] `rg -n "@media \\(max-width: 1440px\\)|@media \\(max-width: 1280px\\)|shopper-shell-row|admin-shell-row" src/styles/globals.css`
- [ ] `npm run typecheck`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The style system exposes every Phase 2 shell/primitives token and enforces the locked kiosk geometry in global utilities
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-and-design-system/02-02-SUMMARY.md`
</output>
