---
phase: 02-app-shell-and-design-system
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/components/primitives/Button.tsx
  - src/components/primitives/IconButton.tsx
  - src/components/primitives/Panel.tsx
  - src/components/primitives/PanelHeader.tsx
  - src/components/primitives/Badge.tsx
  - src/components/primitives/Divider.tsx
  - src/components/primitives/focusRing.ts
  - src/components/primitives/index.ts
  - src/components/primitives/Button.test.tsx
  - src/components/primitives/SurfacePrimitives.test.tsx
  - src/app/shell/ShopperShell.tsx
  - src/app/shell/AdminShell.tsx
  - src/app/shell/ShellModeToggle.tsx
autonomous: true
requirements:
  - SHELL-02
  - SHELL-03
user_setup: []
must_haves:
  truths:
    - Shared primitives expose visible focus treatment and keyboard/mouse-first interaction affordances by default.
    - Shopper and admin proof shells use the same generic primitive layer without leaking domain semantics into the primitives.
    - Shell placeholders remain proof content only while using real reusable components for buttons, panels, badges, and dividers.
  artifacts:
    - src/components/primitives/Button.tsx
    - src/components/primitives/IconButton.tsx
    - src/components/primitives/Panel.tsx
    - src/components/primitives/PanelHeader.tsx
    - src/components/primitives/Badge.tsx
    - src/components/primitives/Divider.tsx
    - src/components/primitives/focusRing.ts
    - src/components/primitives/index.ts
    - src/components/primitives/Button.test.tsx
    - src/components/primitives/SurfacePrimitives.test.tsx
  key_links:
    - The primitives consume the semantic token classes and focus utility introduced by Phase 2 style work rather than raw CSS values.
    - ShopperShell, AdminShell, and ShellModeToggle are refit to use Button, Panel, PanelHeader, Badge, and Divider for shell proof content only.
    - Primitive tests verify interaction semantics and boundary-safe variants before later feature phases reuse them.
---

<objective>
Build the shared primitive layer for Phase 2 and wire the proof shells onto it.

Purpose: Finish the design-system foundation so later phases can compose real screens without inventing ad hoc shell components.
Output: Generic button/surface primitives, shared focus-ring utility, primitive tests, and shell proof content migrated onto the primitive layer.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-and-design-system/02-CONTEXT.md
@.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md
@src/app/shell/ShopperShell.tsx
@src/app/shell/AdminShell.tsx
@src/app/shell/ShellModeToggle.tsx
@src/styles/tokens.css
@src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the generic primitive component layer with explicit variants and shared focus behavior</name>
  <files>src/components/primitives/Button.tsx, src/components/primitives/IconButton.tsx, src/components/primitives/Panel.tsx, src/components/primitives/PanelHeader.tsx, src/components/primitives/Badge.tsx, src/components/primitives/Divider.tsx, src/components/primitives/focusRing.ts, src/components/primitives/index.ts</files>
  <read_first>.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md, src/styles/globals.css, src/styles/tokens.css, src/components/.gitkeep</read_first>
  <action>Create the primitive files under src/components/primitives/ and export them from src/components/primitives/index.ts. Implement Button with the exact variants `primary`, `secondary`, `quiet`, and `destructive`, plus size variants `md` and `lg`. Implement IconButton with `secondary`, `quiet`, and `destructive` variants and the same 48x48 minimum target rule. Implement Panel as a generic bordered surface wrapper, PanelHeader as a title/support/action row, Badge as a generic semantic label with `neutral`, `accent`, `muted`, `destructive`, and `operational` variants, Divider with `horizontal` and `vertical` orientation, and focusRing.ts as a shared utility that returns the class set used for visible focus layering. Use typed props, native button semantics, and className passthrough. Do not add polymorphic 'as' APIs, slot libraries, router links, or domain-specific variants such as `catalog`, `fit`, `health`, or `measurement`.</action>
  <verify>rg -n "primary|secondary|quiet|destructive|neutral|accent|muted|operational|horizontal|vertical" src/components/primitives/*.tsx src/components/primitives/*.ts</verify>
  <acceptance_criteria>
    - `src/components/primitives/Button.tsx` contains `primary`, `secondary`, `quiet`, and `destructive`.
    - `src/components/primitives/Badge.tsx` contains `neutral`, `accent`, `muted`, `destructive`, and `operational`.
    - `src/components/primitives/focusRing.ts` exports a shared utility and none of the primitive files import `zustand`, `react-router`, `@/stores`, or `@/adapters`.
  </acceptance_criteria>
  <done>The project has a mode-neutral primitive layer with fixed variants, typed props, and shared focus behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw shell proof markup with the shared primitives while preserving shell-only boundaries</name>
  <files>src/app/shell/ShopperShell.tsx, src/app/shell/AdminShell.tsx, src/app/shell/ShellModeToggle.tsx</files>
  <read_first>src/app/shell/ShopperShell.tsx, src/app/shell/AdminShell.tsx, src/app/shell/ShellModeToggle.tsx, src/components/primitives/index.ts, .planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md</read_first>
  <action>Refit the Phase 2 shell proof components to consume the new primitives. ShellModeToggle must use Button or IconButton instead of raw button markup. ShopperShell and AdminShell should use Panel, PanelHeader, Badge, and Divider to present shell-only placeholder surfaces in the band, rail, workspace, and inspector regions. Keep the exact proof-content boundary from the approved UI contract: no real shopper/session/admin workflow copy, no measurement/fit/catalog data, and no runtime health messaging. Do not add any new placeholder panels whose labels imply completed feature screens.</action>
  <verify>rg -n "@/components/primitives|<Button|<IconButton|<Panel|<PanelHeader|<Badge|<Divider" src/app/shell/*.tsx</verify>
  <acceptance_criteria>
    - `src/app/shell/ShellModeToggle.tsx` imports from `@/components/primitives`.
    - `src/app/shell/ShopperShell.tsx` and `src/app/shell/AdminShell.tsx` each import at least `Panel` and `Badge`.
    - `src/app/shell/ShopperShell.tsx` and `src/app/shell/AdminShell.tsx` do not contain `Start Session`, `Catalog`, `Measurements`, `Fit`, `Calibration`, `Logs`, or `Health`.
  </acceptance_criteria>
  <done>The shell proof surfaces use the shared primitive layer without crossing the Phase 2 placeholder boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for primitive semantics and shell-safe interaction behavior</name>
  <files>src/components/primitives/Button.test.tsx, src/components/primitives/SurfacePrimitives.test.tsx</files>
  <read_first>src/components/primitives/Button.tsx, src/components/primitives/IconButton.tsx, src/components/primitives/Panel.tsx, src/components/primitives/Badge.tsx, src/components/primitives/Divider.tsx, src/components/primitives/focusRing.ts</read_first>
  <action>Create Button.test.tsx to verify button variant rendering, pressed/disabled semantics as applicable, and focus-class application through the shared focus utility. Create SurfacePrimitives.test.tsx to verify Panel, PanelHeader, Badge, and Divider render with the intended semantic hooks and remain generic rather than domain-specific. Run these tests together with the shell tests from Plan 02-01 so Phase 2 exits with automated coverage for shell composition and shared primitives.</action>
  <verify>npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/components/primitives/Button.test.tsx src/components/primitives/SurfacePrimitives.test.tsx</verify>
  <acceptance_criteria>
    - `src/components/primitives/Button.test.tsx` contains tests for variants and focus behavior.
    - `src/components/primitives/SurfacePrimitives.test.tsx` contains tests for Panel, PanelHeader, Badge, and Divider.
    - `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/components/primitives/Button.test.tsx src/components/primitives/SurfacePrimitives.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The shared primitive layer and the shell proof usage are both covered by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx src/components/primitives/Button.test.tsx src/components/primitives/SurfacePrimitives.test.tsx`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "primary|secondary|quiet|destructive" src/components/primitives/Button.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell proof surfaces run on generic reusable primitives with visible focus affordances and no feature-domain leakage
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-and-design-system/02-03-SUMMARY.md`
</output>
