---
phase: 02-app-shell-and-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/App.tsx
  - src/app/shell/AppShell.tsx
  - src/app/shell/shellMode.ts
  - src/app/shell/ShopperShell.tsx
  - src/app/shell/AdminShell.tsx
  - src/app/shell/ShellModeToggle.tsx
  - src/app/shell/proofContent.ts
  - src/components/shell/ShellFrame.tsx
  - src/components/shell/ShellBand.tsx
  - src/components/shell/StageViewport.tsx
  - src/components/shell/ContextRail.tsx
  - src/components/shell/AdminNavRail.tsx
  - src/components/shell/WorkspaceCanvas.tsx
  - src/components/shell/InspectorRail.tsx
  - src/components/shell/OverlayLane.tsx
  - src/components/shell/index.ts
  - src/app/App.test.tsx
  - src/app/shell/AppShell.test.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
user_setup: []
must_haves:
  truths:
    - The root app renders dedicated shopper and admin shell compositions through mode-aware shell files rather than a single monolithic placeholder.
    - Shopper mode exposes a top shell band, protected center stage, right context rail, and edge-anchored overlay lanes with shell-only placeholder content.
    - Admin mode exposes a command bar, left navigation rail, center workspace, and optional right inspector with operational placeholder content only.
  artifacts:
    - src/app/shell/ShopperShell.tsx
    - src/app/shell/AdminShell.tsx
    - src/app/shell/ShellModeToggle.tsx
    - src/app/shell/proofContent.ts
    - src/components/shell/ShellFrame.tsx
    - src/components/shell/ShellBand.tsx
    - src/components/shell/StageViewport.tsx
    - src/components/shell/ContextRail.tsx
    - src/components/shell/AdminNavRail.tsx
    - src/components/shell/WorkspaceCanvas.tsx
    - src/components/shell/InspectorRail.tsx
    - src/components/shell/OverlayLane.tsx
    - src/app/shell/AppShell.test.tsx
  key_links:
    - src/app/App.tsx retains local ShellMode demo state and passes it to src/app/shell/AppShell.tsx without introducing routing or domain stores.
    - src/app/shell/AppShell.tsx dispatches to src/app/shell/ShopperShell.tsx and src/app/shell/AdminShell.tsx using the shared shell mode contract from src/app/shell/shellMode.ts.
    - ShopperShell and AdminShell compose only the structural shell primitives under src/components/shell/ and consume placeholder copy from src/app/shell/proofContent.ts.
---

<objective>
Create the Phase 2 shell architecture and structural shell primitives on top of the existing Phase 1 scaffold.

Purpose: Lock the mode-aware kiosk shell composition before token expansion and generic primitive work.
Output: Dedicated shopper/admin shell files, structural shell primitives, shell-only placeholder content, and shell composition tests.
</objective>

<execution_context>
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaa/Desktop/Senior Project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/UI_SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-and-design-system/02-CONTEXT.md
@.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md
@.planning/phases/01-foundation-bootstrap/01-03-SUMMARY.md
@src/app/App.tsx
@src/app/shell/AppShell.tsx
@src/app/shell/shellMode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the root shell into dedicated shopper/admin composition files</name>
  <files>src/app/App.tsx, src/app/shell/AppShell.tsx, src/app/shell/shellMode.ts, src/app/shell/ShopperShell.tsx, src/app/shell/AdminShell.tsx, src/app/shell/ShellModeToggle.tsx, src/app/shell/proofContent.ts</files>
  <read_first>AGENTS.md, docs/UI_SPEC.md, .planning/phases/02-app-shell-and-design-system/02-CONTEXT.md, .planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md, src/app/App.tsx, src/app/shell/AppShell.tsx, src/app/shell/shellMode.ts</read_first>
  <action>Keep src/app/App.tsx on local useState<ShellMode> demo wiring only. Replace the monolithic src/app/shell/AppShell.tsx layout with a dispatcher component that renders ShopperShell for 'shopper' and AdminShell for 'admin'. Create src/app/shell/ShellModeToggle.tsx as a shell-demo-only control surface for switching between modes. Create src/app/shell/proofContent.ts with exact shell-only copy constants from the Phase 2 UI contract, including 'Stage Reserved', 'Shell and layout only. Live try-on content arrives in later phases.', 'Context Rail', 'Admin Shell', and 'Operational layout only. Tools and system workflows arrive in later phases.'. Do not add router imports, Zustand stores, screen-state enums, backend copy, or feature-screen copy such as 'Start Session', 'Catalog', 'Fit', 'Calibration', or 'Measurements'.</action>
  <verify>rg -n "ShopperShell|AdminShell|ShellModeToggle|Stage Reserved|Operational layout only" src/app/App.tsx src/app/shell/*.tsx src/app/shell/*.ts</verify>
  <acceptance_criteria>
    - `src/app/App.tsx` contains `useState<ShellMode>('shopper')` and does not import `zustand`, `react-router`, or `@/stores`.
    - `src/app/shell/AppShell.tsx` imports both `ShopperShell` and `AdminShell` and no longer contains the full shell markup for both modes inline.
    - `src/app/shell/proofContent.ts` contains `Stage Reserved` and `Admin Shell` and does not contain `Start Session`, `Catalog`, `Fit Details`, `Calibration`, or `Measurements`.
  </acceptance_criteria>
  <done>The app shell is split into dedicated mode files with shell-only proof content and no feature-state leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Create the structural shell primitive layer under src/components/shell</name>
  <files>src/components/shell/ShellFrame.tsx, src/components/shell/ShellBand.tsx, src/components/shell/StageViewport.tsx, src/components/shell/ContextRail.tsx, src/components/shell/AdminNavRail.tsx, src/components/shell/WorkspaceCanvas.tsx, src/components/shell/InspectorRail.tsx, src/components/shell/OverlayLane.tsx, src/components/shell/index.ts, src/app/shell/ShopperShell.tsx, src/app/shell/AdminShell.tsx</files>
  <read_first>.planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md, src/app/shell/AppShell.tsx, src/styles/globals.css, src/styles/tokens.css</read_first>
  <action>Create named presentational wrappers for ShellFrame, ShellBand, StageViewport, ContextRail, AdminNavRail, WorkspaceCanvas, InspectorRail, and OverlayLane in src/components/shell/. Give each component typed props for className, children, and the minimal semantic hooks needed for aria labels or data-slot attributes. ShopperShell must compose: ShellFrame -> ShellBand -> StageViewport + right ContextRail + top/bottom OverlayLane. AdminShell must compose: ShellFrame -> ShellBand used as command bar -> AdminNavRail + WorkspaceCanvas + optional InspectorRail. Export the structural primitives through src/components/shell/index.ts. Do not import stores, adapters, mocks, or domain types into these components.</action>
  <verify>rg -n "export function ShellFrame|export function ShellBand|export function StageViewport|export function ContextRail|export function AdminNavRail|export function WorkspaceCanvas|export function InspectorRail|export function OverlayLane" src/components/shell/*.tsx</verify>
  <acceptance_criteria>
    - `src/components/shell/index.ts` re-exports all 8 structural shell primitives.
    - `src/app/shell/ShopperShell.tsx` contains `StageViewport`, `ContextRail`, and two `OverlayLane` usages and does not import `AdminNavRail` or `WorkspaceCanvas`.
    - `src/app/shell/AdminShell.tsx` contains `AdminNavRail` and `WorkspaceCanvas` and does not import `ContextRail`.
  </acceptance_criteria>
  <done>The structural shell layer exists as reusable wrappers and the mode-specific shells compose only the correct locked regions.</done>
</task>

<task type="auto">
  <name>Task 3: Add shell composition tests and preserve the root shell smoke coverage</name>
  <files>src/app/App.test.tsx, src/app/shell/AppShell.test.tsx</files>
  <read_first>src/app/App.test.tsx, src/app/shell/ShopperShell.tsx, src/app/shell/AdminShell.tsx, src/app/shell/proofContent.ts, .planning/phases/02-app-shell-and-design-system/02-UI-SPEC.md</read_first>
  <action>Keep the existing root smoke test for mode switching in src/app/App.test.tsx and add a dedicated src/app/shell/AppShell.test.tsx suite for shell composition. Assert that shopper mode renders the shell band, protected stage viewport, right context rail, and top/bottom overlay lanes while excluding admin navigation landmarks. Assert that admin mode renders the command bar, left navigation rail, workspace canvas, and inspector region hooks while excluding shopper-only context rail markup. Add a boundary test that shell proof copy contains only shell-level labels and does not expose shopper feature copy or admin workflow copy.</action>
  <verify>npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx</verify>
  <acceptance_criteria>
    - `src/app/shell/AppShell.test.tsx` contains tests for shopper shell composition, admin shell composition, and placeholder boundary enforcement.
    - `src/app/App.test.tsx` still verifies mode switching through local shell state.
    - `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Shell composition is covered by automated tests and the Phase 2 placeholder boundary is enforced in test code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- --run src/app/App.test.tsx src/app/shell/AppShell.test.tsx`
- [ ] `npm run typecheck`
- [ ] `rg -n "Stage Reserved|Context Rail|Admin Shell" src/app/shell/proofContent.ts`
- [ ] `rg -n "StageViewport|ContextRail|OverlayLane|AdminNavRail|WorkspaceCanvas|InspectorRail" src/app/shell/*.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The root shell is mode-aware, structurally split, and limited to shell-only proof content
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-and-design-system/02-01-SUMMARY.md`
</output>
